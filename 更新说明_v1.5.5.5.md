# CivitaiFreeTool v1.5.5.5 更新说明（2026-08-11）

> 免费 · 全功能 · 无付费墙

---

## 🔧 彻底修复：右键「打开所在文件夹」乱开桌面 / 我的文档

### 为什么反复出问题？
这是 **Windows explorer 的一个经典坑**：

1. 你的桌面本身就运行着一个 explorer 进程
2. 程序调用 `explorer /select,路径` 时，**参数会被转交给这个已运行的进程**处理，而不是新开窗口
3. **转交过程中路径的引号会丢失** → 路径只要含空格（文件夹名或文件名带空格），explorer 就解析失败 → **回退打开桌面/我的文档**（不同系统行为不同，所以一会儿桌面、一会儿我的文档）

### 本次修复（三层保障）
1. **`cmd /c start "" explorer /select,"路径"`**：通过 `start` 启动**独立的 explorer 实例**，引号由 cmd 正确保留，不再经过桌面进程转交 → 带空格路径也能正确打开并选中文件
2. **`os.startfile` 兜底**：万一 explorer 方式失败，自动改用系统级调用直接**打开文件所在文件夹**
3. 前端 toast 反馈：成功显示「已打开文件夹」，失败显示具体原因

> 现在无论你的模型文件夹/文件名有没有空格，右键「📂 打开所在文件夹」都会打开正确的位置。

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v1.5.5.5）
