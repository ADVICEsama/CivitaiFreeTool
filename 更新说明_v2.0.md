# CivitaiFreeTool v2.0.7 发布说明（2026-08-12）

> 上一个版本：v2.0.6 → 本次 **v2.0.7** 修复「关于弹窗最近更新内容过时」

## 🐛 修复：关于弹窗「最近更新」显示旧内容

**现象**：打开「关于」弹窗，标题显示 v2.0.6，但下面的更新条目还是 v2.0.4 时代的老功能（维护清理/歧义词优化…），**没有 v2.0.6 的翻译修复等新内容**。

**修复**：
- 「最近更新」区更新为 **v2.0.6 真实内容**（翻译修复 / 下载三项体验 / 新手引导大修 / 主题与保存按钮）
- 样式升级：条目改为**卡片样式**（每条独立圆角卡片、分区边框、标题加粗主题色、内容超高可滚动），不再是纯文本拼接

已 E2E 验证：弹窗渲染 4 条新条目、含翻译修复、样式生效。

## 📦 下载

- exe：桌面 `工具\CivitaiFreeTool\CivitaiFreeToolWeb.exe`（或 GitHub Releases）
- 源码：main 分支，tag `v2.0.7`

---

# CivitaiFreeTool v2.0.6 发布说明（2026-08-12）

> 上一个版本：v2.0.5 → 本次 **v2.0.6** 修复「翻译成中文」不生效

## 🐛 修复：点击「翻译成中文」翻译不生效

**现象**：详情面板点「🌏 翻译成中文」后，简介/触发词（如 `Trained on Anima-BaseV1`、`I love training Style Loras`）还是英文。

**根因（两处）**：
1. 翻译只写入了 `模型名.json`（WebUI 格式），**没写入 `模型名.civitai.info`**——而详情面板读的是 info → 界面永远显示英文
2. **触发词（trainedWords）根本没翻译**——你看到的正是触发词

**修复**：
- 翻译范围扩大到 **简介 + 触发词**，同时写回 **`.civitai.info` 和 `.json` 两个文件**
- **英文原文全部保留**（新增 `description_zh` / `trainedWords_zh` 字段，不覆盖原文——触发词翻译不影响你在 WebUI 里的英文使用）
- 详情面板：简介显示中文（原文以小字附后）、触发词显示中文（原文小字附后）；**点触发词复制仍是英文原文**（WebUI 需要英文）
- **幂等**：已翻译的模型不会重复翻译（也不会重复调用翻译接口）
- 无百度 Key 时自动回退免费翻译服务；全部失败会明确提示"检查设置 → 百度翻译配置"，不再假装成功

已 E2E 验证：`4pricotA.safetensors`（Trained on Anima-BaseV1 / I love training Style Loras / works well at 1 strength）翻译后 info+json 双写、界面显示中文、复制英文原文、二次翻译跳过。

## 📦 下载

- exe：桌面 `工具\CivitaiFreeTool\CivitaiFreeToolWeb.exe`（或 GitHub Releases）
- 源码：main 分支，tag `v2.0.6`

---

# CivitaiFreeTool v2.0.5 发布说明（2026-08-11）

> 上一个版本：v2.0.4 → 本次 **v2.0.5** 下载相关三项体验升级

## 1️⃣ 批量下载跳转后弹窗提示

点「解析并加入下载队列」跳转到下载管理页时，弹出提示：

> ✅ 已在下载队列中 —— 任务已开始解析并入队，请在本页等待，无需重复点击「解析」。

4 秒自动关闭，也可点「知道了」立即关。

## 2️⃣ 移动分类弹窗显示缩略图

下载完成弹出「是否移动到分类文件夹」时，**左侧显示该模型的缩略图**（同目录封面，没有则显示占位图）——一眼确认是哪个模型，不会移错。

## 3️⃣ 下载管理列表增强

- 列表**左侧新增缩略图列**：已完成的任务自动加载同目录封面（缓存 + 懒加载，不卡列表）
- 行**右键菜单**（与模型管理页对齐）：
  - 📂 打开所在文件夹（资源管理器定位文件）
  - 📋 复制文件名
  - 🌐 打开C站（打开模型主页）

已 E2E 全量验证：提示弹窗出现与自动关闭、移动弹窗缩略图、列表缩略图加载、右键菜单三项与操作。

## 📦 下载

- exe：桌面 `工具\CivitaiFreeTool\CivitaiFreeToolWeb.exe`（或 GitHub Releases）
- 源码：main 分支，tag `v2.0.5`

---

# CivitaiFreeTool v2.0.4 发布说明（2026-08-11）

> 上一个版本：v2.0.3 → 本次 **v2.0.4** 设置页保存按钮改红色

## 🔴 设置页「保存设置」按钮改为红色显眼

- 背景改为红色（跟随各主题的 danger 红色系）
- **文字始终白色**（加粗，任何主题下都不会被主题色覆盖）
- hover 更深红 + 发光阴影，一眼可见

已 E2E 验证：5 个主题（深色/深海/浅色/樱粉/现代）下均为红底 `rgb(229,72,77)` + 白字 + 加粗。

## 📦 下载

- exe：桌面 `工具\CivitaiFreeTool\CivitaiFreeToolWeb.exe`（或 GitHub Releases）
- 源码：main 分支，tag `v2.0.4`

---

# CivitaiFreeTool v2.0.3 发布说明（2026-08-11）

> 上一个版本：v2.0.2 → 本次 **v2.0.3** 调整迷你引导窗位置

## 🧭 迷你引导窗移到右下角「关于」按钮上方

**现象**：点击功能卡片后，缩小悬浮的引导窗出现在**右上角**，被顶部横幅（导航条）挡住了一部分。

**修复**：改为显示在**右下角「关于」按钮的正上方**（底部避让 84px），不与顶部横幅、关于按钮重叠。

已 E2E 验证：卡片底部距视口底 84px、顶部 593px（远离横幅）、位于关于按钮上方不遮挡。

## 📦 下载

- exe：桌面 `工具\CivitaiFreeTool\CivitaiFreeToolWeb.exe`（或 GitHub Releases）
- 源码：main 分支，tag `v2.0.3`

---

# CivitaiFreeTool v2.0.2 发布说明（2026-08-11）

> 上一个版本：v2.0.1 → 本次 **v2.0.2** 修复新手引导交互失效

## 🐛 修复：新手引导交互失效（根因）

**现象**：新手引导第一页的「点击直接进入」点了没反应；主题切换点了也不生效。

**根因（两处同源）**：引导渲染到末尾时用 `body.innerHTML += ...` 追加「跳过引导」按钮——`innerHTML +=` 会把整个引导正文**重新解析重建一遍**，**之前绑定的「点击功能卡片」「点击主题」监听器全部被清空**。所以功能卡点了没反应、主题点了不换。

**修复**：改为 `insertAdjacentHTML` 追加（不重建已有内容），监听器不再丢失。

## 🧭 改进 1：点击功能卡片不再中断引导

- 点击功能卡片 → **跳转到对应页面**，引导**缩小为右下角小窗**（悬浮，显示当前第几步 + 「继续引导 / ✖ 结束」按钮）
- 随时点「▶️ 继续引导」恢复全屏接着走；点「✖」才结束引导

## 🎨 改进 2：主题切换点击即刻应用

- 新手引导 & 设置页下拉：**选主题立即生效**，不用点保存、不整页刷新
- 引导里选的主题会**同步到设置页下拉**，中途点保存设置也不会被旧值覆盖

## 🖥️ 改进 3：保存设置不再白屏刷新

- 移除保存后的 `location.reload()`（整页刷新闪白），改为直接应用主题/缩放/重建表单

## 📐 改进 4：引导弹窗扩大、不再溢出

- 宽度 480px → **720px**（上限 94vw/92vh）
- 10 个主题卡片改为 **5×2 网格**——最右侧的「现代浅色」不再超出画幅
- 步骤条单行不换行——不再出现「反向解 析」折行
- 正文区超高时内部滚动

已 E2E 全量验证：功能卡跳转+迷你窗、主题即时应用、卡片全在视口、步骤条不折行、保存不刷新。

## 📦 下载

- exe：桌面 `工具\CivitaiFreeTool\CivitaiFreeToolWeb.exe`（或 GitHub Releases）
- 源码：main 分支，tag `v2.0.2`

---

# CivitaiFreeTool v2.0.1 发布说明（2026-08-11）

> 上一个版本：v2.0.0 → 本次 **v2.0.1** 修复暗色主题背景发白

## 🐛 修复：新暗色主题背景发白

**现象**：暮紫 / 深海 / 森林 / 熔岩 四个暗色主题的背景都是白色的，只有彩色浮雕/色带有颜色。

**根因**：程序内置的 WebGL 氛围背景（shader.js，就是那层流动色带）判断暗色时只认 `theme === "dark"` 这一个值——新主题 `dark_purple` 等全部被判成浅色，于是画了白色底 + 橙色带，把主题的深色背景盖住了。

**修复**：
1. 暗色判断支持全部 `dark_*` 主题（前缀匹配）
2. **氛围背景彻底跟随主题**：底色改用当前主题的 `--bg`、流动色带改用 `--primary`——
   - 🟣 暮紫 → 深紫底 + 紫罗兰流色
   - 🔵 深海 → 深蓝底 + 天蓝流色
   - 🟢 森林 → 墨绿底 + 翠绿流色
   - 🟠 熔岩 → 深红底 + 橙红流色
   - 亮色系（晴空/樱粉/薄荷）也同步各自底色与流色
3. 切换主题时背景**实时更新**（无需重启）

已 E2E 验证：10 个主题的暗色判定 + 底色 + 流色全部精确匹配。

## 📦 下载

- exe：桌面 `工具\CivitaiFreeTool\CivitaiFreeToolWeb.exe`（或 GitHub Releases）
- 源码：main 分支，tag `v2.0.1`

---

# CivitaiFreeTool v2.0 发布说明（2026-08-11）

> 上一个版本：v1.5.5.1 → 本次 **v2.0.0** 大版本更新

---

## 🎨 1. 十套颜色主题（新增 7 套）

在原有「深色 / 浅色 / 现代浅色」基础上，新增 7 套配色：

| 暗色系 | 亮色系 |
|---|---|
| 🟣 暮紫 | 🔷 晴空 |
| 🔵 深海 | 🌸 樱粉 |
| 🟢 森林 | 🌿 薄荷 |
| 🟠 熔岩 | |

设置 → 界面主题，或新手引导第一步即可切换，实时预览。

## ⚡ 2. 批量下载「点击即反馈」（修复重复下载）

用户反馈：解析等待时间太长，以为没开始 → 反复点击 → **同一个模型下载了好几份**。

本次修复：
- 点击「🔍 解析并加入下载队列」**立即跳转到下载管理页**
- 解析期间按钮变为「解析中...」并**禁用**，解析完成自动恢复
- 任务入队后下载管理页实时显示，不用再干等

## 🏷️ 3. 版本号升级 v2.0

- 软件版本升至 **2.0.0**，关于页动态显示
- 关于弹窗含「最近更新」内容 + **GitHub 一键直达**按钮
- **左上角 logo 点击 = 打开关于页**

## 📦 4. 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 下载 `CivitaiFreeToolWeb.exe`（单文件，免安装），双击即用
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 5. 源码与 README

- 全新 README：功能总览 / 快速开始 / 源码构建 / 更新日志 / License
- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，tag `v2.0`）

---

### 完整功能清单（v2.0）

批量下载（C 站 / HF / 付费到期提醒）· 下载管理（断点续传/校验/移动分类）· 模型管理（多目录/瀑布流/改名/整理/恢复/校验/清理）· 详情面板（图画廊/复制图片/正负提示词）· 反向解析 · 工作流分析 · 10 主题 · 新手引导 6 步 · 到期提醒 · 全量 hover 备注
