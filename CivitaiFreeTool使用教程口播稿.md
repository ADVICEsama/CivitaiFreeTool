# CivitaiFreeTool 使用教程 · 口播稿（v2.0 版）

> 口播风格：手把手教学，从"主题与众不同"切入，然后按真实操作顺序一步步教。
> 【】内为画面/字幕提示（演示软件界面时配字幕更清晰）。

---

## 开场 · 先教你把界面调好看（约 25 秒）

【画面：软件打开，设置 → 界面主题，10 套主题轮播切换】

大家好，今天教大家用 CivitaiFreeTool——一个免费开源的 AI 模型下载、管理工具。

拿到软件第一件事，我建议先**换个主题**，不然浪费了。点右上角「⚙️ 设置」，找到「界面主题」，一共**十套**：暗色系有深色、暮紫、深海、森林、熔岩；亮色系有浅色、晴空、樱粉、薄荷、现代浅色。选中立刻生效，不用点保存——**背景、按钮、连那条流动的色带都跟着变**。

好，界面顺眼了，我们正式开始。

---

## 一、首次打开：走完 6 步引导（约 40 秒）

【画面：新手引导弹窗，逐步演示】

第一次打开软件，会弹出**新手引导**，一共六步，跟着走一遍，后面就顺了：

- 第一步「功能介绍」——六个页面图标，鼠标移上去有说明。注意，**点一下图标可以直接跳进对应页面**，引导会缩成屏幕右下角一个小窗，点「继续引导」随时回来，不会打断你。
- 第二步「选主题」——就刚才那个。
- 第三步「下载目录」——模型下到哪，默认软件目录下，也可以点「📁 浏览」自己选。
- 第四步「API Key」——去 civitai.com 账号页免费生成一个，粘贴进来。**不填也能用**，但建议填上，查询和下载更全。
- 第五步「模型管理目录」——**把你放模型的文件夹填进来**，一行一个。WebUI 和 ComfyUI 分开放的，两个都填。
- 第六步「反向解析」——强烈推荐点「立即体验」，把老模型识别一遍。

填完点「完成」，配置就保存了。以后想改，设置页随时能重开引导。

---

## 二、怎么批量下载（约 40 秒）

【画面：批量下载页，粘贴链接】

接下来教下载。到「📥 批量下载」页：

**第一步，贴链接**。把 C 站模型链接粘进来，比如 `civitai.red/models/xxxx`。**按回车自动换行**，多贴几条一起下。HuggingFace 的链接也支持；如果贴的是 C 站**图片页**链接，它会自动识别图片里用到的所有模型。

【画面：点解析，页面跳转】

**第二步，点「🔍 解析并加入下载队列」**。点了之后**会直接跳到下载管理页**，按钮变成"解析中..."而且锁住——不要慌，这是正常的，等它解析完，任务就会出现在下面的列表里。

【画面：付费弹窗】

如果解析的时候弹出"该模型需付费 / Early Access"——说明它还没到免费期。点「⏰ 加入待办」，软件会自动判断免费时间，到期提醒你；想现在下就点「仍要下载」，但多半会失败，等免费最稳。

---

## 三、下载管理页怎么操作（约 25 秒）

【画面：下载管理页】

下载管理页，任务列表每一行是一个文件，有状态、进度、速度。

- 任务卡住了？点 **🔄 重试选中**。
- 不想下了？点 **🗑️ 移除选中**（不会删已下的文件）。
- 全部暂停了想继续？点 **▶️ 全部开始**——放心，**断点续传**，从断的地方接着下。

【画面：下载完成自动处理】

下载完成它会自动做三件事：生成元数据、下封面图、**问你要不要移动分类**——弹窗选个文件夹，文件就归档过去了。

---

## 四、模型管理：核心教学（约 1 分钟）

【画面：模型管理页，点扫描】

然后是最重要的「🧩 模型管理」。

**第一步，点「🔍 扫描模型」**。它会扫你刚才填的模型目录，扫完每个模型都带封面和 C 站名字——注意，第一次扫可能会等一会儿，模型多的话别急。

【画面：筛选 + 视图切换】

扫完怎么找？上面有筛选框，输入文件名或者 C 站名**实时过滤**；右上角「🖼️ 瀑布流 / 📋 列表」可以切换视图，默认瀑布流就是刚才那个大图卡片。

**改名**：勾选模型（列表左边打勾，或者点「全选」），然后点「✏️ 改名」——二级菜单里选：**自定义改名**、**文件名改成 C 站名**、或者**文件名翻中文**。一次选几百个也能批量改。

【画面：整理模型】

**整理分类**：先把设置页的「🎯 目标环境」选好（WebUI 还是 ComfyUI），再选整理模式——新手建议「手动分类」，它会一个一个弹窗问你把文件放哪；想全自动就选「C 站 tags」，按 `Lora / 基础模型 / 分类` 自动归档。

【画面：恢复误整理】

万一整理错了，别慌——点 **「🔧 恢复误整理」**，它会先给你看预览清单，确认哪些要回退，然后按日志移回去，**只移动、不删除**。

---

## 五、反向解析：把乱码文件认出来（约 30 秒）

【画面：反向解析页，添加文件夹】

现在教**反向解析**——把你硬盘里那些名字看不懂的模型认出来。

到「🔍 反向解析」页，点 **「📁 添加文件夹」**，选你的模型目录（它会自动过滤出模型文件），然后点 **「▶️ 开始反查」**。

【画面：反查结果】

它按 SHA256 去 C 站匹配，表格里状态会从"等待中"变成"完成"——匹配上的，模型名、版本就出来了。反查完回到**模型管理**，点「🔄 刷新」——看，每个模型都有 C 站名字和触发词了，复制就能用。

想自动翻译成中文的，去设置页配一下**百度翻译**（页面里有免费申请教程），反查的时候名字就是中文的。

---

## 六、工作流分析怎么用（约 25 秒）

【画面：工作流分析页，拖入文件】

「🔬 工作流分析」——这个特别简单：

**把 ComfyUI 的工作流 json 文件，直接拖进中间这个虚线框**。带内嵌工作流的 png 图片也能拖。

【画面：三栏结果】

松开手，它自动解析出三块：左边**节点列表**、中间**引用的模型**——重点看这个，它会跟你的本地文件比对，**本地有的标出来，没有的、SHA256 对不上的，一眼就知道缺什么**。最下面是正负面提示词，可以直接抄。

---

## 七、设置页：这几个必看（约 30 秒）

【画面：设置页滚动】

最后说一下设置页几个关键项，按分组：

- **🔑 基本**：API Key、下载目录、模型管理目录——这三个最重要，前面引导填过，这里也能改。
- **🌐 网络**：下载慢或解析失败，开「启用代理」填代理地址；报**证书错误**的，把「启用证书验证」取消勾选就恢复了。
- **⬇️ 下载**：建议把「完成后自动生成 json/info」和「自动下载封面」都开着，不然模型管理里看不到名字和封面。
- **🌏 翻译**：百度翻译 APP ID 和密钥，配了反查自动翻译。
- **📁 分类规则**：目标环境 + 整理模式，整理模型前必须在这选。
- **🧹 维护**：点「删除下载的图片文件夹」能清掉详情页缓存的大图，释放空间。

改完记得点那个**红色的大按钮「保存设置」**——特意做成红的，就是怕你找不到。

---

## 结尾 · 常见问题 + 获取（约 25 秒）

【画面：关于弹窗 / 下载方式】

再回答三个新手必问：

- **扫不出模型？** 检查设置里「模型管理目录」填对没有，模型在子文件夹是正常的，软件会递归扫。
- **下到一半报"暂不可下载"？** 大概率是付费期没到，加待办等免费。
- **配置存在哪？** exe 同目录的 `user_config.json`，换电脑备份它就行。

软件的下载地址我放在简介和评论区，GitHub 也有源码和使用文档。觉得教程有用，**点个关注**，后面还有更多玩法。

这期就到这，我们下期见！

---

## 附：口播时长参考

| 段落 | 时长 |
|---|---|
| 开场（换主题） | ~25s |
| 一、6 步引导 | ~40s |
| 二、批量下载 | ~40s |
| 三、下载管理 | ~25s |
| 四、模型管理（核心） | ~60s |
| 五、反向解析 | ~30s |
| 六、工作流分析 | ~25s |
| 七、设置页 | ~30s |
| 结尾（FAQ + 获取） | ~25s |
| **合计** | **约 5 分钟** |
