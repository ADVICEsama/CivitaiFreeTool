# CivitaiFreeTool v2.1.5 更新说明（2026-08-14 · 修复版）

> 免费 · 全功能 · 无付费墙

---

## 🔧 修复三个问题

### 1. 下载的模型没有触发词信息
- **原因**：默认元数据格式为「SD 扁平 json」，其中触发词字段是 `trained_words`（下划线），而模型管理扫描/详情用 `trainedWords`（驼峰）读取 → 读不到 → 显示「无触发词信息」
- **修复**：
  - 扫描与详情读取时自动兼容两种字段名（`trainedWords` / `trained_words` / `trigger_words`）
  - 生成的 json 同时写入驼峰 `trainedWords` 冗余字段（C 站扩展 / A1111 卡片读取兼容）
- **效果**：已下载的模型重新扫描即显示触发词（KISAK1 等）；新下载的模型 json 双字段齐全

### 2. 模型详情页「打开 C 站」只跳主页
- **原因**：详情面板用 `info.modelId` 拼 C 站链接，但扁平 json 里的字段是 `model_id` → 取不到 ID → 跳 `https://civitai.red/models/`（主页）
- **修复**：兼容读取 `model_id`，正确跳转对应模型页

### 3. 改名后扩展名被清除（.safetensors 丢失）
- **原因**：输入新名时若带点（如前缀 `v1.8 `、日期 `2026.8.14`），程序误把点后内容当扩展名，重命名后主文件没有 `.safetensors` 后缀，模型扫描也识别不到
- **修复**：只有输入以真实模型扩展名（`.safetensors/.ckpt/.pt/.pth/...`）结尾时才视为自带扩展名，否则一律保留原扩展名
- **已损坏文件处理**：之前改名丢扩展名的文件，手动在文件夹里补回 `.safetensors` 后缀即可被重新识别

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v2.1.5）