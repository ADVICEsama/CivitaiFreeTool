# CivitaiFreeTool 更新日志（v1.5.1 → v2.1.24）

> 免费 · 全功能 · 无付费墙

---

## 🎨 主题设置每次启动被打回暗色（v2.1.24）

**现象**（用户反馈）：设置了「樱粉」等主题，每次启动又变回深色。

**根因**：配置加载时有一道主题合法性校验，白名单只写了最早支持的 `dark / light / modern`；
而「暮紫 / 深海 / 森林 / 熔岩 / 晴空 / 樱粉 / 薄荷」这些后来加的主题不在名单里 →
**每次加载配置都被强行改回 `dark`**（保存本身是好的，是启动时被覆盖）。

**改法**：把主题名单抽成 `config.THEMES`（与 `web/style.css` 的 `[data-theme]` 和设置项保持一致），
校验用它；非法值才回退 `dark`；`dark_mode` 旧字段的迁移逻辑保持不变（只对没有 theme 的老配置生效）。

**回归测试**：10 个主题逐个存盘→加载都保持原值；非法值回退；老配置（只有 dark_mode）迁移正常；
用户真实配置读取到 `light_pink` ✅。

---

## ⏰ 到期待办支持一键下载 + 启动卡死兜底提速（v2.1.23）

### 到期提醒清单：看到就能直接下
- 清单每条现在显示**模型名**（新增 label；老条目从链接自动推导），不再是光秃秃一条链接。
- 每条带「**⬇️ 下载**」按钮：点了直接解析并入队（`todo_download`），成功后**自动从清单移除**；
  入队失败则保留并提示原因，不会误删。
- 打开软件时的到期弹窗重做：逐条「一键下载」+「**全部下载**」（只下已到期的）+「去下载页」+「稍后再说」，
  不用再自己复制链接去找。

### 看门狗：卡死判定 45 秒 → 5 秒
- 判定窗口从 45s 缩到 **5s**（健康启动实测只要 2-6 秒），两次超时合计从约 100 秒降到约 **20 秒**内进浏览器模式。
- 防误杀：启动日志 3 秒内还在更新（说明在正常推进）→ 宽限 +3 秒，最多 2 次；真正的卡死不会再写日志，照杀。
- 窗口模式重试给 10 秒；仍无窗口 → 自动切浏览器模式（托盘 + 浏览器界面），下载任务不受影响。

---

## 🔄 换保存文件夹不再把下载停住（v2.1.22）

**现象**（用户反馈）：下载管理里给正在下载的任务换个保存文件夹，下载就被暂停了，还要手动点继续。

**原因**：文件正写到旧文件夹里，要换文件夹就得先把写了一半的 `.part` 搬过去（不能边写边搬），
所以必须先停一下下载线程 → 搬完 → **但旧代码搬完没自动继续**，就停在那儿了。

**改法**：搬完半成品、改好目录后**自动恢复下载**（断点续传，从搬过去的进度接着下）。
整个换目录过程只中断约 1 秒，提示语也改成「已改为 …，下载已自动继续（断点续传）」。

顺带说明两种改法互不干扰：
- **单任务「保存到」列**：正在下载 → 停约 1 秒把半成品搬过去 → 自动续传（剩下的直接下到新文件夹，跨盘也不会下完再复制一遍）。
- **全局目标**（工具栏「📂 设置保存到」/ 设置里的预设）：**不动正在下载的任务**——它们在下完的那一刻自动归位到新目标。

---

## 🖥️ 新增「浏览器模式」：界面卡死不再拖死下载（v2.1.21）

**背景**：窗口偶发启动卡死（WebView2 建窗握手不返回），单进程架构下 GIL 被 .NET 互操作占死，
连下载任务一起僵住。看门狗能自愈，但要等 45 秒且会杀掉整个进程树。

**改法（界面与后端解耦，外观完全不变——同一套 HTML/CSS）**：

1. **通用 RPC 通道**：桥服务新增 `/api/rpc`（127.0.0.1），前端增加 shim——
   浏览器打开时自动把 `window.pywebview.api.xxx` 映射成 `POST /api/rpc`，
   窗口模式走原路（pywebview 注入），两套共用同一份 `web/` 界面与同一套后端方法。
2. **界面文件由本机服务提供**：`GET /` 直接给出 `web/index.html`（防目录穿越，仅本机可达）。
3. **浏览器模式**：设置 → 🎨 界面 → 界面模式 = 浏览器（或启动加 `--browser`）。
   软件在后台常驻（下载照跑），界面在系统浏览器里打开；**刷新/重开页面即可恢复**，
   界面出问题再也不影响任务。窗口模式仍是默认。
4. **托盘图标**（浏览器模式，可关）：左键打开界面，右键菜单 = 打开界面 / 打开下载文件夹 / 退出软件。
5. **关页面后自动退出**（浏览器模式，可开）：靠页面心跳判断；**有任务在下载时不会退**，
   避免「关页面 = 掐断下载」。
6. **看门狗兜底升级**：窗口连续两次启动失败 → 自动改用浏览器模式重启（弹窗告知），
   流程从「卡 45 秒 + 杀整棵树」变成「界面照样能用、下载不断」。

测试：界面文件/穿越防护/RPC/私有方法拦截/心跳 15 项全过；「关页面退出」A（退出）/B（下载中不退）/C（关闭设置不退）
三场景全过；托盘建图标 ok；设置页三选项渲染 ok。

**遗留**：A 方案（窗口做成独立进程，卡死只死窗口、秒级重启）留待后续——现在的浏览器模式已能达到同样的效果。

---

## 🧩 归位逻辑补漏：单任务指定不再被抢、HF 不丢目录（v2.1.20）

v2.1.19 的「完成时自动归位」复查时发现三处问题，全部修掉：

1. **单任务指定的目录会被全局目标抢走**：完成时的自动归位原本无条件按全局目标搬，
   导致用户在某一行单独选的文件夹被覆盖。现在任务带 `dest_explicit` 标记（通过列表「保存到」列设置的会自动打标），
   归位时**以其指定目录为准**，不再搬动。
2. **HuggingFace 任务归位会丢子目录**：HF 下载要保留仓库内的相对结构（如 `sub/deep/x.safetensors`），
   归位统一按「目标 + `hf_rel`」拼，目录结构不再被压平。
3. **老任务不跟随新目标**：v2.1.18 及以前入队的任务把旧目标烤进了 `dest_dir`；
   现在这类任务（无标记）开下时按**当前**全局目标走；清空目标时才回退到它自带的目录。

> 关于「下载完再搬」：这条路一直存在（v2.1.17 起就在完成回调里自动归位），v2.1.19「开下时解析」是它之上的补充——
> 能直接下到目标文件夹就不必下完再搬（跨盘搬 = 整份复制+删原件，几个 GB 要等且要双倍空间）。现在两条路都覆盖：
> 下载前选、下载中选、下载完再选，文件最终都会到指定文件夹。

### 验证（9 项全过）
下到 downloads → 完成归位到目标（含 json 附属）；单独指定的任务留在原地不被抢；HF 任务归位保留 `sub/deep` 结构；
老任务解析用当前目标、清空目标后回退自带目录。

---

## 🎯 修复「选了文件夹但文件还是落在 Lora」（v2.1.19）

### 现场
用户选了文件夹下载，文件仍落在 `…\models\Lora`。查配置发现：
- 全局目标 `download_target_dir` = `…\models\Lora`（早先选的）；
- **根因**：任务在「解析并加入下载队列」那一刻就把落地目录**烤死**了 —— 之后再选文件夹/改目标，对已入队的任务不生效，仍按旧值下载。

### 修复（三层）
1. **落地目录改为「下载开始那一刻」解析**（`Downloader._resolve_dest`）：任务不再在入队时写死目录；
   没被单独指定过目录的任务，开下时用**当前**全局目标 → 默认下载目录；HF 任务保留相对子目录（`info["hf_rel"]` 跟随目标拼接）。
   → 入队之后、开下之前随时改文件夹都能生效。
2. **改全局目标时，把「已完成但落在别处」的文件一并搬过去**（`set_download_target`），并在状态栏说明移动了几个。
   → 用户选文件夹的预期就是「文件到那儿去」，不再出现"选完了文件还在原地"。
3. **下载中改单个任务的文件夹**：不再拒绝，改为**自动暂停 → 等下载线程真正停下（`_active_ids` 清空，防止边写边搬）→ 搬 .part 半成品 → 改目录**，
   用户点「开始」即从断点继续。
4. 下载管理「保存到」列：未单独指定时显示**当前全局目标**并带「默认」小标（所见即所得，不再只显示一个「默认」）。

### 验证（10 项全过）
- 入队后改目标 → 开下时用新目标；改全局目标 → 已完成文件被搬走（原位置清空）；
- 下载中改单任务目标 → 等 1.3 秒（模拟下载线程收尾）后自动暂停、搬 .part、改目录、状态变「已暂停」。

---

## 📁 下载管理：每个文件单独选保存文件夹（v2.1.18）

> 上一版只在「批量下载」页给了全局目标文件夹 + 下载完成后的弹窗，**下载管理列表本身没有分文件夹的入口**，
> 而且完成后的选择还会弹 Windows 系统对话框 —— 这版修正为「下载前就能按文件单独指定」。

### 新增
1. **下载管理列表新增「保存到」列**：显示每个任务将要落地的文件夹（在模型目录内时显示相对路径，如 `Lora\SDXL`）。
   **点这一格**即弹出应用内的模型目录树（与模型管理页同源，**不再用 Windows 系统弹窗**），选完立刻生效：
   - 等待中 / 已暂停 / 失败：改目标；**已有的 .part 半成品一并搬过去**，断点续传进度不丢；
   - 已完成：把文件连 json/封面等附属一起移到新文件夹；
   - 正在下载：提示先「暂停选中」再改（避免下载半途换目录）。
2. **工具栏「📂 设置保存到」按钮**：给勾选的任务批量指定文件夹（没勾选则全部），适合一次下一批、按类分开落地。
3. 下载完成后的「移动分类」弹窗里的选择按钮，也从 Windows 系统对话框改为同一个应用内文件夹树（风格统一）。

### 验证
- 后端：等待中（半成品跟随搬移）、已完成（文件+附属移动）、下载中（拒绝并提示暂停）、非法/不存在目录（拒绝）、任务持久化 —— 12 项全过。
- 前端：真实页面自检 —— 表头出现「保存到」、单元格渲染、点击 → 文件夹树 → 对 `t1` 单独生效、批量按钮对两个任务一起生效，全部通过。

---

## 📥 下载落地文件夹 + 🧬 SHA256 查重（v2.1.17）

### 1. 下载页直接选存储位置（不再靠下载完成弹窗）
- **批量下载页**新增「📥 保存到：<路径>」行：点「选择文件夹」从模型目录树里挑（与模型管理页同一套文件夹数据），
  之后下载的模型（连 json/封面）**直接落进该文件夹**，不再弹「移动分类」询问；「用默认」一键改回。
- **设置 → 下载 → 下载目标文件夹（预设）**：同一功能常驻预设（界面里带「选择 / 清除」）。
- 三者联动：设了目标文件夹后，`ask_move_after_download` 的弹窗自动不再出现；
  老任务/扩展/HF 下载完成后也会自动归位到该文件夹（`_handle_dl_done` 兜底）。
- 目标文件夹必须在模型目录内（或默认下载目录内），选到别处会被拒绝，防误设。

### 2. SHA256 查重（模型管理 → 🧬 查重）
- 后台多线程算全库 SHA256（线程数用设置里的「哈希线程数」），按哈希分组找重复；
- 每组**自动挑选保留哪一份**（路径层级最浅优先，即直接放在 Lora/Stable-diffusion 这类类型目录下的 → 其次体积大 → 路径短）；
- 结果弹窗列出每组「保留 ✅」与可清理副本（默认勾选副本），支持全选/全不选，确认后**逐文件移入回收站**（可还原），
  走的是和右键删除同一条 `rm_file` 链路（含 json/预览图等附属）。
- 最终清理数量与可释放空间在弹窗标题和状态栏显示。

### 验证
- 后端：临时目录造「同内容双份 + 独一份」验证分组/保留策略/拒绝非法目录（全过）。
- 前端：用桩 API 在真实页面里跑自检——下载页选择弹窗（4 条目带层级）、设置行选择、查重弹窗 → 二次确认 → `rm_file` 实际被调用（全过）。

---

## 🐶 看门狗再加固：心跳防 PID 复用 + 重启前清孤儿 WebView2（v2.1.16）

v2.1.15 之后现场又实测到两次真卡死（22:43、22:47），第二轮验证暴露两个新问题：

1. **PID 复用会让实例漏挂看门狗**：看门狗的 PID 被杀后很快被系统回收给无关进程，
   新实例据此误判「看门狗已在跑」→ 跳过拉狗 → 那个实例一旦卡死就没人救（22:43 那次就是）。
   **修复**：看门狗每 2 秒写心跳时间戳，`watchdog_alive()` 要求「PID 存活 **且** 心跳 < 25 秒」才算活着。
2. **卡死残留的孤儿 WebView2 会拖累下一次启动**：重启前先按命令行里的
   `CivitaiFreeToolWeb\wv2` 精确清掉属于本工具的孤儿 msedgewebview2（绝不碰其它 WebView2 应用）。

现场验证（22:47 那次真卡死）：`no window after 46s → kill tree + relaunch` → 新实例 6 秒后
`window visible, healthy`，窗口正常出现 —— 自愈链路端到端打通。

---

## 🧯 看门狗重启链路的真实坑（v2.1.15）

v2.1.13 上线外部看门狗后**当场实测到一次真卡死**（22:36 启动后窗口没出现），看门狗按预期判定并杀进程重启，
**但重启的实例崩溃、旧进程还弹了警告框**。抓到两条对话框原文：

```
Unhandled exception in script:
  Failed to execute script 'pyi_rth__tkinter' due to unhandled exception:
  Tcl data directory "C:\...\Temp\_MEI120642\_tcl_data" not found.
Warning:
  Failed to remove temporary directory: C:\...\Temp\_MEI120642
```

### 根因
PyInstaller（onefile）用环境变量 `_PYI_APPLICATION_HOME_DIR`（及 `_MEIPASS`/`_PYI_ARCHIVE_FILE`/
`_PYI_PARENT_PROCESS_LEVEL`）把**解包目录**传给子进程。原实现用 `dict(os.environ)` 拉起新进程 →
两个后遗症：① 看门狗会复用宿主（app）的解包目录并一直占着它 → 宿主 bootloader 清理失败弹警告框；
② 重启的实例以为自己还在老目录里 → 启动即崩（Tcl 数据找不到）。

### 修复
`watchdog_ext.clean_env()`：拉起任何新进程（看门狗自己、看门狗重启的实例）前**剥掉全部 PyInstaller 解包目录变量**，
让新进程老老实实重新解包。主程序拉看门狗、看门狗重启宿主，两处都已切换。

---

## 🔁 断流自动重试 + 版本选择修复 + 改名去符号（v2.1.14）

### 1. 下载断流自动重试 + 断点续传（SSL EOF 一类报错不再直接失败）
**症状**：`<urlopen error [SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol>`
**原因**：C 站文件经代理节点下载，节点/线路会掐断 TLS 连接（握手或传输中途都可能）；旧代码单次失败即报错，
已下好的几百 MB 白扔。
**修复**：下载改为「退避重试 + 断点续传」——失败后 2/4/8/16/30 秒重试，每次用 `Range` 从 `.part` 已有字节继续；
并且补上**静默断流检测**（有 Content-Length 但字节数不足时判为断流去重试，而不是当作下载完成去报「SHA256 校验失败」）；
「收尾被掐但数据已收全」直接完成，不再多打一次请求。重试次数见设置「断流自动重试次数」（默认 5，0=不重试）。
不可重试的错误（HTTP 403/404、C 站付费限制、证书错误、真哈希不符）仍立即失败。

### 2. 修复「选了非最新版却下载最新版」
**原因**：URL 正则要求 `?modelVersionId=` 紧跟模型 ID，而真实 C 站链接是
`/models/<id>/<slug>?modelVersionId=<vid>`（带 slug），匹配不到 → 代码回退取 `modelVersions[0]`（最新版）。
**修复**：版本号改为从查询串解析（与位置/顺序无关），带 slug、多参数、只有版本号的链接都能正确取到所选版本。

### 3. 报错一键复制
下载管理里标了下划线的报错单元格、批量下载的解析日志区：**点一下即复制**全文到剪贴板（状态栏提示），方便直接去搜。

### 4. 一键改名 / 下载命名：清理特殊符号（ComfyUI 逗号坑）
ComfyUI 会把文件名里的逗号当提示词分隔符，`（py,ill,xl）` 这类名字会导致找不到 lora 报错。
设置 → 下载 → 「文件名清理符号」：不清理 / 去逗号 / 去逗号+括号 / 去逗号+括号+横线下划线→空格（默认去逗号+括号）。
同时作用于**下载命名**与模型管理的**「改成 C 站名」一键改名**（连附属 json/预览图一起改）。

---

## 🩺 外部看门狗：进程内自愈为什么救不了、现在怎么救（v2.1.13）

### 复现与根因（py-spy 进程栈转储实测）
卡死时用 `py-spy dump` 抓栈，现象固定：

| 线程 | 卡点 | 说明 |
|---|---|---|
| 主线程 | `pywebview/winforms.py:834` `thread.Join(500)` 循环 | 等 STA 线程建窗，永远等不到 |
| STA 线程 | `pywebview/edgechromium.py:120` `EnsureCoreWebView2Async` | .NET 互操作卡住不返回 |
| 内置看门狗线程 | `main_web.py` 的 `time.sleep(2)` 后无法继续 | **GIL 被上面那个 .NET 调用占死** |
| 桥接/HTTP 线程 | 全部 idle | 同样被 GIL 饿死 → health 超时、CLOSE_WAIT |

结论：WebView2 的 browser/gpu/network/renderer 子进程其实**全部起来了**，卡的是宿主窗口的 WinForms 建窗握手；
卡住时 pythonnet 的 .NET 互操作把 **GIL 永久占住**，进程内所有 Python 线程一起饿死。
所以 v2.1.12 的进程内看门狗**连「超时」日志都写不出来**，自动重启从未发生——自愈形同虚设。
（单实例保护仍然有效：卡死时只有一个实例，不再叠加进程。）

### 修复
1. **新增独立进程看门狗 `watchdog_ext.py`**（纯 stdlib + ctypes，**绝不导入 pywebview/clr**）：
   宿主启动 45 秒内没出现可见顶层窗口 → 杀掉宿主整棵进程树（含全部 WebView2 子进程，逐 PID 精确杀，
   看门狗自己排除在外）→ 重新拉起；再失败 → 再次重启 + 原生提示框给出处理指引；之后保持监控不再重启风暴。
   进程有自己的 GIL 和线程，宿主再死也影响不到它。
2. **主程序以 `--watchdog` 参数拉看门狗**：该分支在任何重量级导入**之前**分流（见 `main_web.py` main 开头），
   保证看门狗进程完全不碰 .NET。
3. **进程内原来的自动重启已移除**，只保留窗口里程碑日志（它在此死锁下不可靠，留着会与外部狗抢着重启）。
4. **不误杀**：只在「宿主进程活着但 grace 秒内没有任何可见顶层窗口」时才动手；窗口一旦出现即回到健康态；
   宿主正常关闭 → 看门狗自行退出，不留残留；PID 复用有进程名校验兜底。

### 📋 日志
- `%LOCALAPPDATA%\CivitaiFreeToolWeb\startup.log` —— 宿主启动里程碑（含「external watchdog spawned」）
- `%LOCALAPPDATA%\CivitaiFreeToolWeb\watchdog.log` —— 看门狗判定与动作（healthy / stage0 / stage1 / 退出）
- `%LOCALAPPDATA%\CivitaiFreeToolWeb\app_pid.txt`、`watchdog_owner.json` —— 二者握手用的状态文件

---

## 🛡️ 启动自愈：有后台无前台彻底兜底（v2.1.12）

### 背景
v2.1.11 解决了多开叠加，但**单实例偶发卡死**仍会导致「有后台进程、无窗口」——根因是本机 WebView2 (Chromium) 环境初始化偶发挂起（显卡/虚拟显示适配器环境下常见），卡在窗口出现之前。

### 修复（三重保险）
1. **启动看门狗**：启动后 30 秒内主窗口未出现 → **自动重启一次**（环境变量防循环）→ 仍失败则弹窗给出处理指引。**从此任何情况下窗口都会出现，或明确提示**
2. **WebView2 数据目录改为按 PID 独立**（`%LOCALAPPDATA%\CivitaiFreeToolWeb\wv2\<pid>`）：彻底消除残留/并发浏览器进程对固定目录的锁争用（这是间歇性卡死的元凶之一）；退出自动清理，7 天前的异常残留自动删除
3. **禁用 WebView2 GPU 渲染**（`--disable-gpu`）：规避显卡/虚拟显示驱动异常导致的 Chromium 初始化挂死；本工具界面为本地页面，软件渲染无感

### 📋 启动日志
- 每次启动写入 `%LOCALAPPDATA%\CivitaiFreeToolWeb\startup.log`（里程碑式：单实例→导入→Api→建窗→看门狗），再遇到问题可直接查看

---

## 🧱 启动健壮性加固（v2.1.11）

### 🐛 修复：打开很多后台进程却没有前端窗口
**现象**：双击 exe 后任务管理器出现多个 `CivitaiFreeToolWeb.exe`，但一直没有窗口。
**根因**：单实例检查原先在 `import pywebview / webui` **之后**才执行——一旦启动过程卡住（例如临时目录被大量旧解压残留塞满、杀软实时扫描拖慢 WebView2 初始化），后续每次双击都不会触发退出，而是不断叠加新的卡死进程。
**修复**：
- **单实例互斥体检查提前到任何重量级导入之前**：第二次双击在任何情况下都会立即退出（或激活已有窗口），彻底杜绝进程叠加
- 互斥体命名空间增加 `Local\` 回退（`Global\` 创建失败时仍能去重）
- **WebView2 数据目录固定**到 `%LOCALAPPDATA%\CivitaiFreeToolWeb\webview`：不再每次启动用临时目录，规避临时目录堆积 / 杀软扫描导致的启动卡住，且与其他 pywebview 应用隔离

### 🧹 运维提示
- 若已遇到「无窗口 + 很多后台进程」：任务管理器结束所有 `CivitaiFreeToolWeb.exe`，清理 `%TEMP%` 下残留的 `_MEI*` 目录后重开
- 本版本起即使启动再慢，多开也会立即被去重，不会再叠加

---

## ✨ 整理模型全新设计（v1.5.2）

### 🐛 修复：整理后 ComfyUI / Forge 读不到模型
**根因**：旧整理逻辑无条件按「类型 + 基础模型」两层拼接目标目录，不检查模型是否已在类型目录下——已在 `Lora\` 里的模型被再移到 `Lora\SDXL\`、`Lora\Pony\` 等嵌套子目录；模型移走后空目录被整个删除（`Lora\` 目录消失，ComfyUI/Forge 自然读不到）；Checkpoint 还被放进了 Forge 不认的 `Checkpoint\` 目录（应为 `Stable-diffusion\`）。

### 📁 整理功能三模式
- **🎯 目标环境前置**：整理前必须先选择 WebUI/Forge 或 ComfyUI（目录名不同：`Lora/loras`、`Stable-diffusion/checkpoints`），未选择时按钮拦截提示，防止再乱移
- **手动分类**（默认）：逐个弹窗选择目标文件夹，完全由你决定
- **C 站 tags 自动分类**：按模型 info 里的 C 站分类标签（Style / Pose / Character / Anime / Clothing / Concept / Backgrounds 等 15 类）分类到 `<类型目录>/<基础模型>/<分类>` 两级目录——光辉、noob、anima 等基础模型作第一级，pose / style 作第二级
- **自定义规则**：关键词 → 文件夹（原有功能）

### 🛡️ 安全兜底
- 无匹配 tags 的模型**保持原位不动**
- animatediff、onnx、clip、optical flow 等未知类型目录**一律不碰**
- 类型根目录（`Lora/`、`loras/` 等）**永不删除**

### 🔧 恢复误整理（新功能）
- 选择环境 + 自动分类模式后，工具栏出现「🔧 恢复误整理」按钮
- 每次整理自动记录 `organize_log.json` 移动日志（时间/源/目标）
- 恢复 = 日志反向回退 + 扫描非标准目录，**预览清单后确认执行**，只移动不删除，json / 封面随行

---

## 🔧 修复与优化（v1.5.1）

### 🐛 修复
- **工作流分析**：拖入大文件解析报「Maximum call stack size exceeded」栈溢出 → 改为迭代解析
- **瀑布流**：切换到瀑布流后无法切回列表视图
- **图片下载**：下载全部图片时新图出现在 6-9 位、2-4 位空占位 → 顺序与占位修复
- **图片右键菜单**：详情图右键菜单无法弹出（变量作用域 ReferenceError 崩溃）→ 提升为全局变量
- **右键菜单定位错位**：缩放后菜单位置偏移 → 迭代校正 + rAF 二次校准双保险
- **作者显示去重**：详情页只保留一个可点击复制的作者徽章

### ✨ 优化
- **确认弹窗按钮位置**：默认「确定」在左、「取消」在右（防误触），设置页可一键翻转回旧布局
- API Key / 百度密钥输入框改为**密码框**（默认隐藏），带**小眼睛**切换明文

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）
