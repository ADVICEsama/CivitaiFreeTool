# CivitaiFreeTool 更新日志（v1.5.1 → v2.1.49）

> 免费 · 全功能 · 无付费墙

---

## 🔧 改名 Split Button 合并 + 文件夹筛选 + 更新页列宽/Sticky（v2.1.49）

- **改名按钮合并为一体控件**：外层唯一边框 + 内部细分隔线，整体 hover/整体打开态；
  下拉箭头改为 SVG（打开时 CSS 旋转 180°，不再改文字节点）。
- **文件夹筛选（新）**：「全部文件夹 ▼」下拉，按模型所在文件夹过滤（数据复用现成 `get_folders`，只拉平做下拉）；
  原「文件夹显示」正名为**「文件夹显隐」**（功能不变，避免与筛选混淆）。
- **排序控件更明确**：下拉选项统一「排序：xxx」；方向按钮改为 SVG 图标 + 「升序/降序」文字（不再是孤立的 ↑）。
- **更新页**：列宽改比例（模型信息 32% / 当前版本 10% / 可用版本 23% / 发布时间 11% / 状态 10% / 操作 14%），
  宽屏自然扩展；**表头 Sticky**（滚动时列名常驻）；长文本一律省略号。
- **状态徽章带图标**：有更新/仅换底模/无法判定/未检查/已是最新 全部换成统一 SVG 图标 + 文字。
- 详情面板「自定义封面」按需求改名 **「设置封面」**。

测试：Split Button 结构 / 文件夹筛选 / 箭头 SVG+CSS 旋转 / 状态图标 / 列宽与 Sticky 共 47 项，全过。

---

## 🚑 紧急修复：v2.1.47 详情/文件夹面板被补丁误伤（v2.1.48）

**问题**：v2.1.47 我重做"模型详情"面板时，替换锚点写得太宽（`panel.innerHTML =` 在文件里先出现在
"文件夹面板"函数里），结果把两块代码整段切坏：

- `showModelDetail()` 函数丢失 → **双击模型卡片没有任何反应**（控制台报未定义函数）；
- `fmRender()`（文件夹面板）被塞进了详情面板的 HTML → **点"文件夹显示"也没反应**。

**修复**：从 v2.1.46 的干净版本精确恢复这两段代码（恢复原文件夹面板 + 复原详情函数的取数逻辑），
**同时保留 v2.1.47 的新详情 UI 与全部图标**；重建后逐项验证 9 个关键函数齐全、调用点 8 处完好。

**教训**：替换锚点必须带函数名等唯一上下文，替换后必须验证"函数存在 + 能跑"，
不能只看语法检查通过（`node --check` 对"整段错位"是查不出来的）。

---

## 🎨 三页统一 Metro + SVG 图标系统 + 详情页 Inspector + 更新页修复（v2.1.47）

### ① Dropdown 彻底加固（实体不透明 + portal）
- 菜单打开时**移动到 body（portal）+ fixed 定位**：任何祖先的 overflow / transform / stacking context 都裁不到它；
- 菜单**背景强制 #fff 实体不透明**（opacity/backdrop 一律 none），叠在内容之上，不再透出下层文字；
- 新增全局层级令牌 `--z-content/toolbar/dropdown/popover/modal/toast`，不再各处随手写 z-index；
- 点击菜单内部不再误关；窗口 resize 时已打开菜单自动重新定位。

### ② 全站 SVG 图标系统（去 Emoji）
- 新增内联 SVG sprite（25 个线性单色图标：search/refresh/scan/check/layers/download/upload/pencil/folder/shield/info/settings/more/external/image/file/tag/trash/x/list/grid/globe/copy/sort/ruler），
  统一 1.7 描边、currentColor、随主题着色；
- **模型管理 / 模型详情 / 更新** 三页的按钮、菜单、区块标题全部换图标；顶部导航同时去 Emoji；
- 代理按钮同步标签时**保留图标**，并自动剥掉动态文案里的 Emoji（如「⏹ 停止检查」）。

### ③ 模型详情 = 桌面工具 Inspector（重做）
- 结构：顶部标题栏（名称 + 作者 + × 关闭）｜左：大图（限高 44vh）+ 方形缩略图（选中蓝框）｜右：结构化信息；
- 右侧改为一屏 Inspector：标题 + **轻量 Meta 行**（类型/底模/版本/文件，不再一排彩色胶囊）+
  **触发词区**（点击复制，带复制图标，去掉长说明）+ 简介 + **分组操作**（主要操作 / 文件 / 信息处理 / 图片）；
- 11 个功能按钮 id 全部保留，业务逻辑零改动。

### ④ 更新页表格修复
- 日期列 nowrap + 固定宽（不再断行）；操作列 nowrap + 固定宽，按钮超长省略号 + tooltip；
- 可用版本列最小宽度（不再挤压）；行高统一 56px；当前版本加粗、可用版本保持下拉；
- 顶部筛选栏：搜索框加宽、按钮统一 34px、检查更新仍是主按钮。

测试：Dropdown portal / 不透明 / 内部点击 / z 令牌 / 图标交叉引用 / 详情结构与 id / 更新页列规则 共 45 项，全过。

---

## 🐞 修复「下拉菜单点不开」+ Metro 二期精修（v2.1.46）

### 严重 Bug：改名/整理/检查/信息处理 的菜单无法展开
根因（两个叠加）：JS 把 `.open` 类加在**外层容器**上，而 CSS 的显示规则写的是 `.mm-menu.open`
（要求类加在菜单自身）——两边对不上，菜单永远不显示；另外 `.card { overflow:hidden }` 还会**裁切**菜单。
修复：CSS 改为 `.btn-group.open .mm-menu { display:block }` 与 JS 对齐；工具栏解除裁切（overflow:visible）
并提升层级（工具栏 z-index 40 / 菜单 120，内容层 1）。完整交互链路（按钮→容器→菜单→菜单项）已用桩 DOM 实测。

### 精修
- **按钮三级权重**：`扫描模型` 唯一蓝色主按钮；`刷新/检查更新/更新选中/去更新` 白底细边框（常用级）；
  `改名/整理/检查/信息处理/更多` 低权重点击才显色；统一高度 36px、统一字号与内边距。
- **菜单打开态反馈**：按钮持续浅蓝高亮 + 箭头翻转（▾ → ▴），关闭即复原。
- **留白压缩**：标题/工具栏/筛选栏内边距各减 4~6px，瀑布流更早进入视野。
- **空封面优雅处理**：新增占位状态机 —— 载入中 / 暂无封面 / 封面加载失败（统一 `◇` 占位图，高度稳定 190px），
  图片加载成功才显示，不再出现白块或"像程序坏了"的观感。
- **瀑布流响应式列宽**：column-width 280px → 1366≈4 列 / 1920≈6 列 / 2560≈8 列（超宽屏加列而不是放大卡片）。
- **卡片信息两行**：`底模 · 类型` + `版本 · 大小`，与你的示意图一致。
- **视图切换改成分段控件** `[☰ 列表][▦ 瀑布流]`，激活态蓝底白字，默认仍是瀑布流。
- **关于按钮**：固定右下角留 18px 边距，Metro 化（白底细边框，去阴影去放大），内容区底部预留空间不压卡片。

测试：Dropdown 链路 9 项 + 精修项 12 项 + 主题防回归 26 项，全过。

---

## 🟦 Metro 主题 v2：从「传统 Win10 控件」改为「现代扁平 Metro」（v2.1.45）

v2.1.44 的 Metro 用的是"正版 Win10 控件"配色（灰底 + 深描边实心按钮），观感偏"传统 Windows 管理软件"。
这一版按"现代 Metro 应用"重做组件层（旧主题/其它 10 套主题完全不受影响）：

- **按钮**：去掉灰底与深描边 → **纯色块 + 简洁文字 + 轻微 hover**
  （次要操作 = 透明底，悬停出现**非常浅的蓝**；`扫描模型` 是唯一高饱和 Windows 蓝主按钮）
- **Checkbox**：不再用浏览器默认 → **自绘方框**（白底浅灰描边，选中 = 蓝底白勾，完全直角）
- **Select**：去掉浏览器默认外观 → 自绘下拉箭头 + 浅描边 + 聚焦时底部 2px 蓝色条
- **输入框**：浅描边 + 聚焦蓝色底边（不再深灰粗框）
- **瀑布流卡片**：大图为主体，底栏**只留 3 行文字**（模型名 / 底模·类型·版本 / 大小），
  状态只在图上留一个徽章（有更新 = ❗ 可点去 C 站；已最新 = 绿色小标）；文件名放到悬停提示里
- **卡片 hover**：只换 1px 边框为蓝（无阴影、无放大、无渐变）
- **下拉菜单**：白底 + 极浅描边 + 直角 + 浅蓝 hover + 选中项蓝色小标记
- **列表视图**：去掉重描边，行分隔线极浅（#f4f4f4），hover 浅蓝、选中浅蓝 + 左侧 2px 蓝条
- **筛选栏/标题**：扁平化（计数变成素文字，不再药丸）

测试：作用域零泄漏 / 自绘控件 / 扁平化断言 / 卡片克制 / 默认瀑布流 共 26 项，全过。

---

## 🟦 全新独立主题：Metro（Win10 磁贴风）+ 模型管理顶部重构（v2.1.44）

### 新主题 Metro（独立于现有 10 套主题，随时可切回）
- 直角化：按钮/输入框/表格/卡片/弹窗全部 0~2px 圆角（原来 8~14px 大圆角 + 胶囊按钮）
- Win10 蓝 #0078D4 主色 + 白底 + 浅灰工具栏 + 深灰文字；Segoe UI 字体
- 去掉大阴影/渐变：扁平、明快、工具软件感（不是网页 Dashboard 感）
- 独立 CSS（`web/metro.css`），全部规则限定在 `[data-theme="metro"]` 作用域，**不污染其它主题**
- WebGL 背景在 Metro 下静音（保持纯色背景，省 GPU）

### 模型管理页顶部重构（22 个按钮 → 分组）
- 第一层：`[扫描模型] [刷新] [检查更新]`
- 第二层：`[改名 ▾] [整理 ▾] [检查 ▾] [信息处理 ▾] [⋯ 更多]`，右侧 `[更新选中] [去更新]`
- 新增筛选行：搜索 + 底模 + 状态 + 排序 + 升降序
- 改名＝"主按钮 + 小角 ▾"（点主按钮走默认动作，点小角选方式：自定义 / 改C站名 / 翻中文 / 改名设置）
- 视图切换保持原来的单按钮翻转（默认仍是瀑布流）
- 新增底模/状态/排序筛选与瀑布流卡片信息（C站名 · 类型 · 底模 · 版本 · 大小 · 有更新/已最新徽章）

### 实现方式（零业务逻辑改动）
- Metro 工具区的按钮全部是**代理**：点击转交给隐藏的老按钮 `click()`，
  标签/禁用/显隐用 MutationObserver 跟随（如「⏹ 停止检查（N/M）」）。
- 老按钮区在 Metro 下隐藏、在其它主题下**原样保留**（两个主题互不干扰）。

测试：34 个 id 全保留 / 代理交叉引用 / 菜单与改名项 / CSS 作用域零泄漏 / 卡片徽章 共 16 项，全过。

---

## ✨ 更新页表头可点击排序（v2.1.43）

你说得对，之前那几列只是"显示"，点上去没反应。现在：

- **模型信息 / 当前版本 / 同底模可用版本 / 发布时间 / 状态** 五列全部可点表头排序，
  点一下按该列排、再点一下切换升/降序，表头会显示 ▲/▼ 箭头（当前排序列高亮）。
- 各列默认方向按"最有用"来：**发布时间**默认新的在前、**可用版本数**默认多的在前，
  其余（名称/当前版本/状态）默认升序。
- **当前版本**用语义化比较：v1 < v1.1 < v1.2 < v1.10 < v2（不是字符串序，v1.10 不会跑到 v1.2 前面）。
- **状态**按 有更新 → 仅换底模 → 无法判定 → 未检查 → 已最新 排序，一屏内先看最要紧的。
- 顶部原有的"排序"下拉与表头**联动**（改一个另一个跟着变），点击排序后自动回到第 1 页。
- 每列悬停有说明（例如"点击按可选版本数量排序"）。

测试：5 列排序结果 + 升降序切换 + 默认方向 + 状态含未知/仅换底模的情形 + HTML 结构 共 24 项，全过。

---

## 🐛 修：更新报错 skipped 未定义 + 关不掉的弹窗 + 新增错误日志（v2.1.42）

- **修 `cannot access local variable 'skipped'`**：更新下载统计"跳过"的计数变量忘了初始化（我上一版打补丁时静默失败了）。
  影响：凡是走到"跳过"分支的更新，都会被误报成失败（下载其实成功了）。现在跳过的正常计入"跳过 N 个"。
- **修"关不掉的弹窗"**：提示类弹窗（"这些没能加入下载队列 / 没能清理"）以前用的是**确认框且没等它返回**，
  于是点"确定"只关了遮罩、内容框永远留在屏幕上。现在改用专门的**提示框（只有一个「知道了」，点它就关，点遮罩也关）**。
- **新增错误日志系统（你问的）**：
  - 路径：`%LOCALAPPDATA%\CivitaiFreeToolWeb\error.log`（超过 2MB 自动滚成 error.log.1）
  - 覆盖：主线程未捕获异常、子线程异常、**RPC 调用异常**（带完整 traceback）、**前端 JS 错误**（window.onerror / 未处理的 Promise）
  - 入口：**设置页新增「📂 打开日志文件夹」按钮**（同目录还有 startup.log / watchdog.log）
  - 以后遇到问题：点这个按钮，把 error.log 发出来即可定位。

测试：跳过不再报错且计入跳过 / 错误日志写入与滚动 / 前端与桥接上报 / 两个弹窗可关闭 共 13 项，全过。

---

## 🐛 严重修复：勾选检查会把其他模型的更新记录清空（v2.1.41）

**现象**：点「检查更新（只查勾选）」后，其他模型的更新信息全没了，只能重新全局扫。

**原因**：检查结束写缓存时用的是**整体替换**（`self._updates = {...}`）。全量检查时没问题，
但"只查勾选"只产出一部分记录，替换就把其余模型全冲掉了。

**修复**：
- 结束写回改为**合并**（勾选检查只覆盖它查的那几条，其余原样保留）；
- 「上次全量检查时间」只在**全量**检查时更新，勾选检查不再动它（不会再误导 24 小时缓存判断）；
- 顺手：写回时**剔除文件已不存在的记录**（删掉的模型不再在更新页留幽灵行）。
- 中断保存（「⏹ 停止」）本来就是合并逻辑，保持不变。

测试：勾选 2 个不影响其他 8 条 / 勾选 1 个不影响其他 9 条 / 全量时间戳不被改动 /
已删文件的记录被剔除 / 源码层面不存在整体替换写法 共 13 项，全过。

---

## ✅ 勾选后「检查更新」= 只检查勾选的（v2.1.40）

- 更新页/模型管理页里**勾选了条目**时，点「⏫ 检查更新」**只检查勾选的这几个**（几十秒就完），
  不再每次都全量跑一遍（288 个模型视网络要 5~40 分钟）。
- 勾选检查会**忽略 24 小时缓存**（显式选择 = 立即重查）和**更新白名单**（你点名要查的，就查）。
- 没勾选时行为不变：全量检查（受 24 小时缓存保护，会提示"要重查请点强制刷新"）。
- 勾选的条目缺少 C 站信息时会明确提示（先跑一次「🔍 反向解析」即可）。

测试：只查勾选 / 跳过 24h 缓存 / 全量仍受缓存与白名单约束 / 无信息时的提示 / 前端两处传参 共 17 项，全过。

---

## ⏹ 检查更新可以中断了（v2.1.39）

- 检查更新跑起来后，**「⏫ 检查更新」按钮会变成「⏹ 停止检查（N/M）」**（更新页和模型管理页都是），
  再点一次即停：当前这一条跑完就收尾，**已经检查出来的结果会保存**，不用从头再来。
- 停止后可以随时重新点「检查更新」继续（会从第一组重新过一遍，但已有结果不会丢）。
- **查重（哈希计算）同样支持中断**（排队中的任务直接取消，不占 CPU）。
- 以后长任务不再"只能等它跑完"。

测试：中途停止 / 保存部分结果 / 标志重置后可重启 / 前端按钮切换 共 15 项，全过。

---

## ⏫ 去更新入口 + 降级支持 + 未检查状态（v2.1.38）

### 模型管理页 →「⏫ 去更新」
工具栏新增按钮：**点一下直接跳到「⏫ 更新」页，并默认只看有更新的（❗）模型** —— 不用再自己去筛。
（普通点导航栏进更新页时，保持你上次的筛选不变。）

### 新增「降级」：不只想升，还可以回到你想用的那一版
- 「同底模可用版本」下拉里本来就有**所有**版本（包括比你现在旧的），现在**选中比当前旧的版本时**：
  - 按钮自动变成 **「降级到 vX」**，并用**暖色**样式和「更新到」区分开；
  - 提示语也改成"回退到这一版（比你当前用的旧）"。
- 选回当前版本 → 按钮自动消失（没事可做）。
- **本地版本已从 C 站下架**（显示"无法判定"）的条目：以前完全没有下拉；现在会列出 C 站**现存的全部版本**，
  方便你挑一版换过去 —— 这类情况恰恰最需要降级。
- 降级下载同样走：新版下到旧版所在文件夹、入队防重复、跳过已存在、旧版按设置保留或回收。

### 新下载的模型显示为「◻ 未检查」（不再误标"已是最新"）
- 只有**真正检查过**（记录里带检查时间）且没新版的条目才显示「✅ 已是最新」；
  扫到但没检查过的显示「◻ 未检查」——状态筛选里也能单独筛「未检查」。
- 更新页现在把**扫描到的模型也纳入列表**：刚下载完的模型会立刻出现（状态"未检查"），
  点「⏫ 检查更新」即可补齐版本信息，不用等缓存刷新。

测试：前端 15 项（未检查状态 / 扫描并入 / 更新与降级文案切换 / 选回当前版本隐藏按钮 / 去更新入口）
+ 回归 89 项，全过。

---