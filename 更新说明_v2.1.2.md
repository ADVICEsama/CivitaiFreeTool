# CivitaiFreeTool v2.1.2 更新说明（2026-08-14 · 修复版）

> 免费 · 全功能 · 无付费墙

---

## 🔧 Chrome 扩展一键下载链路修复

### 1. 点击「开始下载」后，软件自动打开「下载管理」页
- 之前：确认下载后软件页面没有反应，要自己点「下载管理」才能看到任务
- 现在：**入队成功瞬间，软件自动切到「下载管理」页并弹出「✅ 已在下载队列中」提示**，任务进度实时可见

### 2. 解析改为同步返回，失败原因直接显示在小窗里
- 之前：确认后立即显示「已开始下载」，但解析在后台进行，失败时（如网络不通/代理未开）软件里根本没有任务，用户以为没生效
- 现在：扩展小窗**等待解析完成再反馈**（约 1~5 秒）：
  - ✅ 成功 → 显示「已开始下载」
  - ❌ 失败 → 小窗直接显示具体原因，如：
    - 「连接 Civitai 超时…需要代理：在设置页勾选启用代理…」
    - 「被 Civitai 拒绝访问（403）…」
    - 「模型没有可用版本」等

### 3. 修复隐藏 Bug：SHA256 参数名错误
- 之前「仍要下载」（受限模型付费下载）路径因 `DownloadTask` 参数名写错，**入队必然失败**
- 现在已修正为 `expected_sha256`，该路径恢复正常

---

## 使用流程（v2.1.2）

1. 打开 CivitaiFreeTool.exe，在 Chrome 打开 C 站模型页
2. 点扩展图标 → 确认小窗 → 「⬇️ 开始下载」
3. 解析成功后：小窗显示 ✅ + 软件**自动切到「下载管理」页**显示进度
4. 下载完成自动生成 json + 封面

> 如遇「连接超时」提示：软件 设置 → 勾选「启用代理」并确认代理地址（如 127.0.0.1:7897）

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v2.1.2）
