# CivitaiFreeTool v1.5.5.1 更新说明（2026-08-10）

> 免费 · 全功能 · 无付费墙

---

## 🔐 修复：测试 API 报「SSL 证书过期」

### 问题现象
代理设置好后测试 API，弹出：
```
[SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed:
certificate has expired (ssl.c:1032)
```
C 站网页能正常打开，但工具一直解析不了任何模型。

### 排查结论（已实测）
- **C 站证书本身没有过期**：实测 `civitai.red` 证书由 Google Trust Services 签发，还有 46 天有效期
- **根因**：你的代理软件开启了解密（HTTPS 抓包 / MITM 模式），返回的是**代理自己的证书**——浏览器信任它（你装过代理证书），但 Python 不信任 → 报「证书过期」

### 修复方案
1. **设置 → 🌐 网络 新增「启用证书验证」开关**（默认开启）
   - 在你信任当前网络/代理的前提下，**取消勾选** → 跳过 TLS 证书验证 → 测试 API 与解析立即恢复
2. **测试 API 遇 SSL 错误时给出中文诊断**，明确提示三种可能原因与解决步骤：
   - 代理软件开启了解密（HTTPS 抓包/MITM）→ 关闭该功能，或在软件里取消勾选「启用证书验证」
   - 系统时间不正确 → 校准系统时间
   - 证书问题 → 更新代理软件

> ⚠️ 提醒：「跳过证书验证」仅在**你信任当前网络环境**时使用（如自家代理）；公共 Wi-Fi / 陌生网络请保持开启。

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库已清理开发临时文件并补充 `.gitignore` / `requirements.txt`，可直接 `git clone` 后 `pip install -r requirements.txt` 运行 `python main_web.py`
