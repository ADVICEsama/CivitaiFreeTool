# CivitaiFreeTool v2.1.4 更新说明（2026-08-14 · 修复版）

> 免费 · 全功能 · 无付费墙

---

## 🔧 修复：网络/代理相关（SSL 连接中断、封面下载失败）

### 1. 封面缩略图下载改走代理
- **问题**：下载封面（`.preview.png`）一直用直连，而 C 站图片 CDN 在国内被墙 → 封面下载静默失败 → 模型管理/下载管理/移动分类弹窗都没有缩略图（即使用手动批量下载也一样）
- **修复**：封面下载与模型/API 请求一样**走代理**（跟随 设置 → 代理 配置），并遵循「证书验证」开关
- **影响位置**：下载完成自动封面、模型管理「下载封面」、旧版界面封面下载——全部统一

### 2. SSL 连接中断错误的中文提示
- **问题**：遇到 `SSL: UNEXPECTED EOF WHILE READING` 时直接显示原始技术报错，看不懂
- **修复**：现在翻译为可操作提示：
  > 连接 Civitai 被中断（SSL/网络错误）。可能原因：代理节点不稳定或已失效（切换节点后重试）；网络波动（稍后重试）；若已开启代理仍频繁出现，可尝试关闭「证书验证」…

### 排查建议（若仍频繁 SSL 中断）
1. 在代理软件（Clash 等）里**切换节点**——本错误多为节点失效/波动
2. 确认 设置 → 网络 里「启用代理」勾选且地址正确（如 127.0.0.1:7897）
3. 仍频繁出现 → 关闭「证书验证」再试（仅在你信任当前网络时）

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v2.1.4）
