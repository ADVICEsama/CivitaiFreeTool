# CivitaiFreeTool 使用指南（v2.0.1）

> 免费 · 全功能 · 无付费墙 —— Civitai / HuggingFace 模型下载、管理、反向解析工具
> 适用版本：v2.0 / v2.0.1（界面如有出入以你使用的版本为准）

---

## 目录

1. [软件是什么](#1-软件是什么)
2. [安装与首次启动（新手引导）](#2-安装与首次启动新手引导)
3. [页面总览](#3-页面总览)
4. [📥 批量下载页](#4-批量下载页)
5. [📁 下载管理页](#5-下载管理页)
6. [🧩 模型管理页](#6-模型管理页)
7. [🔍 反向解析页](#7-反向解析页)
8. [🔬 工作流分析页](#8-工作流分析页)
9. [⚙️ 设置页](#9-设置页)
10. [模型详情二级界面](#10-模型详情二级界面)
11. [其他界面：到期提醒 / 关于 / 右键菜单](#11-其他界面到期提醒--关于--右键菜单)
12. [常见问题 FAQ](#12-常见问题-faq)

---

## 1. 软件是什么

CivitaiFreeTool 是一站式 AI 模型管理工具，核心解决三件事：

- **下载**：从 Civitai（civitai.red / civitai.com）和 HuggingFace 批量下载模型，支持断点续传、并发下载、付费模型到期提醒
- **管理**：把你本地几万个模型文件整理得明明白白 —— 缩略图、C 站名字、触发词、改名、分类、校验
- **反查**：把已经下载但不知道是什么的模型文件，自动识别出它的 C 站信息（名字、触发词、类型、封面）

**软件形态**：单文件免安装（`CivitaiFreeToolWeb.exe`），双击即用。所有配置保存在 exe 同目录的 `user_config.json` 里（**里面含你的 API Key，请勿分享该文件**）。

---

## 2. 安装与首次启动（新手引导）

1. 从 GitHub Releases 或网盘下载 `CivitaiFreeToolWeb.exe`，放到任意文件夹（建议单独建一个文件夹，例如 `C:\CivitaiFreeTool`）
2. 双击运行 —— 第一次打开会弹出 **👋 欢迎使用** 引导，共 6 步：

| 步骤 | 内容 | 说明 |
|---|---|---|
| 1️⃣ 功能介绍 | 六个页面各司其职 | 鼠标移到每个图标上会显示功能简介，**点击可直接进入对应页面**；下方有 GitHub 仓库直达 |
| 2️⃣ 选择主题 | 10 套配色 | 深色 / 暮紫 / 深海 / 森林 / 熔岩（暗色系）+ 浅色 / 晴空 / 樱粉 / 薄荷 / 现代浅色（亮色系），点击即实时预览 |
| 3️⃣ 下载目录 | 模型下载后存放的位置 | 默认软件根目录下 `downloads/models`，可点「📁 浏览」选择任意文件夹 |
| 4️⃣ API Key | Civitai 免费 API Key | 点「📚 如何注册 API」看图文教程或「🌐 打开注册页」直达。**不填也能用**，但模型查询/部分下载受限 |
| 5️⃣ 模型管理目录 | 你本地存放模型的地方 | **每行填一个**。WebUI 和 ComfyUI 分开存放的就把两个目录都填上（如 `D:\sd-webui-forge-neo\webui\models` 和 `D:\ComfyUI\models`），扫描时会合并显示 |
| 6️⃣ 反向解析 | 强烈推荐 | 把你已下载的模型识别出 C 站信息。点「🚀 立即体验」直接进入反向解析页；也可以「⏭️ 跳过」，以后随时可用 |

> 💡 每一步底部都有「⏭️ 跳过引导」—— 不想填的直接跳过，已填的内容都会保存。以后想重新配置，在 **设置页 → 新手引导** 可随时重开。

**跑通最小流程（3 分钟上手）**：
1. 引导里选好主题、下载目录、模型管理目录
2. 到 **📥 批量下载** 粘贴一个 C 站模型链接 → 点「解析并加入下载队列」
3. 到 **📁 下载管理** 看它下载完成
4. 到 **🧩 模型管理** 点「扫描模型」—— 模型带着封面和名字出现了

---

## 3. 页面总览

顶部胶囊导航共有 **6 个页面** + 右侧状态胶囊：

```
┌──────────────────────────────────────────────────────────┐
│ (logo) 📥批量下载 📁下载管理 🧩模型管理 🔍反向解析 🔬工作流分析 ⚙️设置   [就绪] │
└──────────────────────────────────────────────────────────┘
```

- **点击 logo** = 打开「关于」弹窗（版本号、最近更新、GitHub 直达）
- **右侧状态胶囊**（如「就绪」「扫描完成：36 个模型」）实时显示当前操作状态
- 背景的流动色带是动态氛围背景，会跟随你的主题配色（设置页可关闭）

---

## 4. 📥 批量下载页

**入口**：顶部导航第一个标签。这是全软件的主入口。

### 4.1 支持三种链接

| 链接类型 | 示例 | 行为 |
|---|---|---|
| Civitai 模型页 | `https://civitai.red/models/1290339` | 解析模型 → 加入下载队列 |
| HuggingFace | `https://huggingface.co/xxx/yyy`（仓库或文件直链） | 弹出文件选择，勾选要下的文件 |
| Civitai 图片页 | `https://civitai.red/images/12345` | 识别图片用到的所有模型 → 批量加入队列 |

### 4.2 操作步骤

1. **输入链接**：在输入框粘贴链接，**回车自动新建下一行**，可以一次粘贴很多条
2. 快捷按钮：
   - **📋 粘贴剪贴板链接** —— 一键读取剪贴板里的所有链接
   - **➕ 添加一条** —— 手动加一行空输入框
   - **🗑️ 清空全部** —— 清空所有待解析链接
3. 点 **🔍 解析并加入下载队列**：
   - **立即跳转到下载管理页**（不用干等）
   - 解析期间按钮变「解析中...」并**禁用**，防止重复点击导致重复下载
   - 解析日志显示在下方区域（`[OK]` / `[失败]` 逐条列出）

### 4.3 付费 / Early Access 模型

如果某个模型需要付费或还在 Early Access 期，解析后会弹窗提示，两个选择：

- **⏰ 加入待办（自动到期提醒）** —— 软件自动判断 C 站免费时间，到期打开软件时提醒你
- **💳 仍要下载** —— 强行下载（通常会失败，因为 C 站还没开放文件）

> 免费时间到期前如果想查看待办，点批量下载页的 **⏰ 到期提醒** 按钮。

---

## 5. 📁 下载管理页

**入口**：顶部导航第二个标签。查看和控制所有下载任务。

### 5.1 工具栏

| 按钮 | 作用 |
|---|---|
| ▶️ 全部开始 | 开始下载所有未完成的任务 |
| ⏸️ 暂停选中 | 暂停勾选的任务（下次继续，**断点续传**） |
| 🔄 重试选中 | 重试失败的任务 |
| 🗑️ 移除选中 | 从列表移除（**不会删已下载的文件**） |
| 🧹 清空已完成 | 清掉所有完成项 |
| 💾 保存任务列表 | 把当前任务列表保存下来（重启软件后任务还在） |

### 5.2 任务表格

```
文件名          状态      进度     速度     大小       错误
─────────────────────────────────────────────────────────
xxx.safetensors 下载中    45%      8.2MB/s  1.2GB
yyy.safetensors 已完成    100%     -        340MB
zzz.safetensors 失败      -        -        -         HTTP 403: Forbidden
```

- 点击行首可**勾选/取消勾选**（配合批量暂停/重试/移除）
- **状态**：等待中 / 下载中 / 已完成 / 失败 / 已暂停
- **下载完成自动处理**（设置里可开关）：
  - 自动生成元数据（`模型名.civitai.info` + WebUI 可读的 `模型名.json`）
  - 自动下载 C 站封面图（作为模型管理的缩略图）
  - 询问是否**移动分类**：弹窗让你选目标文件夹，把文件归档到该去的地方
- **SHA256 校验**：下载完成后自动校验文件完整性，不一致会报错并清除缓存重新下
- **防坑**：如果下载到的是网页而不是文件（付费未开放/链接失效），软件会检测到并报「暂不可下载」，**不会把 HTML 当模型写进磁盘**

---

## 6. 🧩 模型管理页

**入口**：顶部导航第三个标签。**最核心、功能最多的页面**。

> 使用前提：在设置页或引导里填了「模型管理目录」，然后点 **🔍 扫描模型**。

### 6.1 第一行工具栏

| 按钮 | 作用 |
|---|---|
| 🔍 扫描模型 | 扫描所有模型管理目录，读取元数据 + 封面 |
| 🔄 刷新 | 重新扫描（不读元数据，只刷新列表，更快） |
| ✅ 校验完整性 | 计算文件哈希与 C 站比对，检查文件是否完整/损坏 |
| ✏️ 改名 | **二级菜单**：自定义改名 / 文件名改成C站名 / 文件名翻中文 |
| 📄 生成SD可读json | 生成 WebUI 能识别的 `模型名.json` 元数据 |
| 🖼️ 下载封面图 | 把 C 站第一张预览图下载到模型目录当缩略图 |
| 🌏 翻译成中文 | 把 C 站模型简介翻译成中文（需设置百度翻译） |
| 📤 识别模型信息 | 把选中的模型送去反向解析，匹配名字/触发词/封面 |

### 6.2 第二行工具栏

| 按钮 | 作用 |
|---|---|
| 📂 整理模型 | 按规则把选中模型移到分类文件夹（**需先在设置选 🎯 目标环境**，否则按钮会提示） |
| 🔧 恢复误整理 | 只在「目标环境已选 + 整理模式非手动」时出现——按移动日志**预览并回退**误整理的模型（只移动不删除） |
| 📁 文件夹显示 | 下拉面板：**逐文件夹开关显示/隐藏**（不想要 animatediff、clip 这些文件夹可以藏起来） |
| 🗑️ 清理冗余文件 | 删除失效的 info、孤儿封面等多余文件 |

### 6.3 工具行（列表筛选区）

- **筛选框**：按 文件名 / C站名 / 路径 实时过滤
- **全选 / 清除勾选 / 反选**：批量勾选操作
- **🖼️ 瀑布流 / 📋 列表**：切换两种视图
- **已勾选 N 个**：实时显示勾选数量

### 6.4 列表视图

表格列（**表头可以点击排序**；**右键表头可以勾选显示/隐藏列**）：

| 列 | 说明 |
|---|---|
| ☑️ | 勾选列（点表头全选） |
| 图 | 缩略图 |
| 文件名 | 本地文件名 |
| C站模型名 | 从 C 站匹配的名字 |
| 类型 | LORA / Checkpoint / VAE... |
| 基础 | SDXL / Pony / NoobAI... |
| 版本 | 模型版本号 |
| 更新 | C 站更新时间 |
| 哈希 | SHA256（前几位） |
| 大小 | 文件大小 |
| 下载时间 | 文件本地时间 |
| 路径 | 完整路径 |

### 6.5 瀑布流视图

大图卡片平铺（像 C 站主页），每张卡片显示：缩略图 + 文件名 + C站名 + 类型角标。右上角显示勾选状态。

### 6.6 行右键菜单（列表 & 瀑布流通用）

| 菜单项 | 作用 |
|---|---|
| 📋 复制文件名 | 复制本地文件名 |
| 🀄 复制C站模型名 | 复制 C 站上的模型名（不改本地文件） |
| 📂 打开所在文件夹 | 资源管理器定位到该文件 |
| 🌐 打开C站 | 浏览器打开该模型的 C 站主页 |
| ✏️ 改名 | 自定义改名 |
| 🏷️ 文件名改成C站名 | 一键统一成 C 站名字 |
| 📄 生成SD可读json | 生成 WebUI json 元数据 |
| 🀄 文件名翻中文 | 本地文件名翻译成中文 |
| 📤 识别模型信息 | 送反向解析 |
| 📂 整理模型 | 移动分类 |
| 🗑️ 移入回收站 | **进回收站（可还原），不是永久删除** |

> 所有按钮/菜单项鼠标悬停都会弹出**白色小字说明**，拿不准就 hover 一下。

### 6.7 双击模型 = 打开详情（见第 10 节）

---

## 7. 🔍 反向解析页

**入口**：顶部导航第四个标签。把已下载的模型文件识别出 C 站信息。

### 7.1 操作步骤

1. 点 **📂 添加文件**（单选）或 **📁 添加文件夹**（一次加整个目录，自动过滤模型文件）
2. 点 **▶️ 开始反查** —— 软件计算每个文件的 SHA256，去 C 站匹配
3. 表格实时更新状态：`等待中 → 反查中 → 完成 / 未找到`

### 7.2 表格列

| 列 | 说明 |
|---|---|
| 文件 | 本地文件名 |
| SHA256 | 文件哈希（匹配依据） |
| 状态 | 等待 / 反查中 / 完成 / 未找到 |
| 模型 | 匹配到的 C 站模型名 |
| 版本 | 模型版本 |

### 7.3 反查结果去哪了？

- 生成 `模型名.civitai.info` —— **模型管理页**刷新后，模型就有 C 站名字、触发词、封面了
- 如果设置了百度翻译，名字/简介会**自动翻译成中文**
- 匹配不到的（冷门/被删模型）会标记「未找到」，不影响使用

### 7.4 其他按钮

- ⏸️ 暂停 / ⏹️ 停止 —— 反查量大时随时停
- 🗑️ 移除选中 / 🧹 清空全部 —— 清理列表
- 筛选框 —— 按文件名/状态/模型过滤

> 小技巧：反查是按 SHA256 精确匹配，**改名、移动文件都不影响再次反查**。

---

## 8. 🔬 工作流分析页

**入口**：顶部导航第五个标签。分析 ComfyUI 工作流。

### 8.1 使用方法

1. 把 ComfyUI 的 `.json` 工作流文件（UI 格式或 API prompt 格式）**拖进虚线框**
2. 或者拖入带内嵌工作流的 `.png` 图片（在 ComfyUI 里点「Save」保存的图）
3. 也可以点「**点击选择**」用文件对话框选

### 8.2 分析结果（三栏）

| 区块 | 内容 |
|---|---|
| 🧩 节点列表 | 工作流里的所有节点 |
| 🤖 引用的模型 | 工作流用到的模型，**自动和本地模型匹配**（含 SHA256 比对，一眼看出本地有没有） |
| 💬 提示词 | 正面 / 负面提示词 |

> ⚠️ 目前只支持 ComfyUI 工作流格式；A1111/Forge 的 PNG 参数暂时无法解析。

---

## 9. ⚙️ 设置页

**入口**：顶部导航第六个标签。所有配置都在这里，**点「保存设置」生效**。每个分组标题（legend）可以点击折叠。

### 9.1 🔑 基本

| 设置项 | 说明 |
|---|---|
| Civitai API Key | 免费申请，用于查询模型信息、下载与反向解析（`civitai.com/user/account` 生成） |
| 下载目录 | 模型下载后存放位置 |
| 模型管理目录 | **每行一个**，WebUI 和 ComfyUI 分开存放就都填上 |
| 站点域名 | civitai.red / civitai.com 切换（网络异常时换着用） |

### 9.2 🌐 网络

| 设置项 | 说明 |
|---|---|
| 启用代理 | 走代理（科学上网工具），解析/下载失败时可试 |
| 启用证书验证 | **代理开了 HTTPS 解密报证书错误时，取消勾选即可恢复** |
| 代理地址 | 如 `127.0.0.1:7897`（Clash 默认端口） |
| 并发下载数 | 同时下载任务数，越大越快但占带宽 |
| 下载超时(秒) | 无响应超过该秒数判定失败并重试 |
| 哈希线程数 | 计算哈希（校验/反查用）的线程数 |

### 9.3 ⬇️ 下载

| 设置项 | 说明 |
|---|---|
| 完成后自动生成 json/info | 没有它，模型管理看不到名字/触发词 |
| 完成后自动下载封面 | 模型管理显示缩略图用 |
| 完成后询问移动分类 | 下载完弹窗让你选目标文件夹归档 |
| metadata 格式 | `sd` = WebUI 直接识别的扁平 json；`civitai` = C 站原始结构；`both` = 两个都生成 |

### 9.4 🌏 翻译

| 设置项 | 说明 |
|---|---|
| 百度翻译 APP ID / 密钥 | 免费申请（百度翻译开放平台），用于翻译模型名/简介 |
| 反向解析自动翻译 | 反查时自动翻译 |
| 下载文件名为中文 | 下载时把模型名翻成中文当文件名 |

### 9.5 🎨 界面

| 设置项 | 说明 |
|---|---|
| 界面主题 | **10 套**：暗色系 深色/暮紫/深海/森林/熔岩 + 亮色系 浅色/晴空/樱粉/薄荷/现代浅色 |
| 界面缩放 | 80% ~ 150%（也可以 Ctrl+滚轮 快捷缩放） |
| 改名默认动作 | 点「改名」默认执行：自定义 / 改成C站名 / 翻中文 |
| 确认弹窗按钮翻转 | 交换「确定/取消」位置防误点 |
| 启动默认页 | 打开软件默认进哪个页面 |
| 模型默认视图 | 瀑布流 / 列表 |
| 模型列表斑马纹 | 行间变色便于对齐 |
| 顶部氛围动态背景 | 关闭后是纯色背景，性能更好 |

### 9.6 📁 分类规则（整理模型的前提）

| 设置项 | 说明 |
|---|---|
| 🎯 目标环境 | **必须选**：WebUI/Forge（Lora、Stable-diffusion 目录）或 ComfyUI（loras、checkpoints 目录）。不选的话整理按钮会提示 |
| 整理模式 | 手动（逐个弹窗让你选文件夹）/ C站tags（按 C 站分类自动两级归档，需先反查生成 info）/ 自定义规则 |
| 自定义规则 | 每行一条：`关键词1, 关键词2 -> 目标文件夹`（如 `NoobAI, noob -> NoobAI`） |

> 例：目标环境=WebUI + 模式=C站tags → 扫描后整理，LORA 会按 `Lora/<基础模型>/<分类>` 归档（如 `Lora/NoobAI/Pose/`），无 tags 的模型**保持原位不移动**，未知类型（animatediff 等）**一律不动**。

### 9.7 底部按钮

- **保存设置** —— 必点
- **测试 API 连接** —— 快速验证 API Key 和网络通不通
- **测试百度翻译** —— 验证翻译配置
- **📖 新手引导** —— 随时重开首次引导

---

## 10. 模型详情二级界面

在模型管理页**双击任意模型**（列表行或瀑布流卡片）打开。

### 10.1 顶部大图 + 缩略图画廊

- 大图展示当前图片，下方缩略图可切换（**本地图集优先**：`模型名.images/` 文件夹里的图 + 本地封面；没有本地图就显示 C 站示例图）
- 大图上**右键**：
  - **🖼️ 复制图片** —— 把图片本身复制到剪贴板（可直接粘贴到微信/PS）
  - **💬 复制正面提示词** —— 复制该图的正向提示词（**本地 PNG 元数据优先识别**，很准）
  - **🙅 复制负面提示词** —— 同上，负面

### 10.2 信息区

显示：文件名、C站模型名、类型、基础模型、版本、简介（可翻译）、触发词（可复制）。

### 10.3 操作按钮

| 按钮 | 作用 |
|---|---|
| 🌐 打开C站 | 浏览器打开该模型主页 |
| ✏️ 改名 | 自定义改名 |
| 📤 识别模型信息 | 送反向解析 |
| 🌏 翻译成中文 | 翻译简介（需百度翻译） |
| 🀄 文件名翻中文 | 本地文件名汉化 |
| 🖼️ 下载所有示例图 | 把 C 站全部示例图下到 `模型名.images/` 文件夹（本地画廊） |
| 📄 生成SD可读json | 生成 WebUI json 元数据 |

---

## 11. 其他界面：到期提醒 / 关于 / 右键菜单

### 11.1 ⏰ 到期提醒

- 打开方式：批量下载页右上角「⏰ 到期提醒」按钮
- 用途：记录**需要等免费/到期**的模型（Early Access / 付费期）
- 时间**自动判断**：加入时软件从 C 站读取免费时间，到期打开软件自动提醒你

### 11.2 关于浮窗

- 打开方式：**右下角「ℹ️ 关于」浮标**，或**点击左上角 logo**
- 内容：版本号（动态显示）、最近更新内容、**GitHub 一键直达**按钮

### 11.3 右键菜单（模型管理）

见第 6.6 节 —— 列表行和瀑布流卡片右键都有完整操作菜单，每项带 hover 说明。

---

## 12. 常见问题 FAQ

**Q1：模型管理点「扫描模型」报错 / 扫出来是 0 个？**
- 先检查「模型管理目录」填对没有（设置 → 基本）
- 如果目录里模型在子文件夹（如 `Lora/`、`Stable-diffusion/`）是正常的，软件会递归扫描
- 不想看到的文件夹（animatediff、clip 等）用「📁 文件夹显示」逐项隐藏
- 点「🔄 刷新」重试；还是不行就把报错文字发给我

**Q2：下载报「暂不可下载：C 站付费 / Early Access」？**
- 该模型还在付费期，还没到免费时间。点「⏰ 加入待办」等它免费了自动提醒你
- 如果是网络问题，检查代理设置（设置 → 网络）

**Q3：打开软件全是白底？**
- 这是 v2.0.0 的一个 bug（新暗色主题被氛围背景误判成浅色），**v2.0.1 已修复**，请更新 exe

**Q4：报「证书验证失败」？**
- 你的代理工具开了 HTTPS 解密（MITM），到 **设置 → 网络 → 取消勾选「启用证书验证」** 即可

**Q5：模型没有名字 / 没有触发词 / 没有封面？**
- 先确认下载时勾了「完成后自动生成 json/info」和「自动下载封面」
- 老文件可以全选 → 点「📤 识别模型信息」送反向解析，反查完刷新就有名字和封面了

**Q6：下载到一半停了，能续传吗？**
- 能。重新点「▶️ 全部开始」或「🔄 重试选中」，自动从断点继续（.part 缓存）

**Q7：改名会不会影响我的 WebUI 使用？**
- 会——WebUI 通过文件名识别 LoRA/Checkpoint，**改名后记得在 WebUI 里刷新**。反向解析/生成 info 不会受影响（按 SHA256 匹配）

**Q8：整理模型把文件移走了，想恢复？**
- 用「🔧 恢复误整理」按钮：会按移动日志**预览**将要回退的文件，确认后只移动不删除

**Q9：配置/API Key 存在哪？**
- exe 同目录 `user_config.json`。**重装/换电脑时备份这个文件即可**，不要随意分享

**Q10：如何获取帮助 / 反馈 bug？**
- GitHub Issues：https://github.com/ADVICEsama/CivitaiFreeTool/issues
- 作者 B 站 / 粉丝群：见软件关于弹窗

---

*本文档随软件版本持续更新。祝使用愉快！🎉*
