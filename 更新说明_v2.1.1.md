# CivitaiFreeTool v2.1.1 更新说明（2026-08-14）

> 免费 · 全功能 · 无付费墙

---

## 🆕 Chrome 扩展「一键下载」——浏览器直达下载

在 Chrome 里浏览 Civitai 模型页时，点击扩展图标弹出**确认小窗**，确认后模型**直接开始下载**，不用复制链接、不用回软件点解析。

### 流程
1. 打开 CivitaiFreeTool（v2.1.1+），再在 Chrome 打开 C 站模型页
2. 点击扩展图标 → 确认小窗显示**模型名 + 链接** → 点「⬇️ 开始下载」
3. ✅ 提示已开始下载 → 软件「下载管理」页实时显示进度，完成后自动生成 json + 封面

### 细节
- ✅ **浏览器内确认小窗**：显示当前页模型名（自动读取页面标题）、链接、一键下载按钮
- ✅ **状态分流**：非模型页 → 提示；软件未打开 → 提示「未连接」；失败 → 显示具体原因（可重试）
- ✅ **右键直达**：页面/链接右键 → 「一键下载到 CivitaiFreeTool」（跳过确认窗，角标 ✓）
- ✅ 支持模型页 / 版本链接（`?modelVersionId=`）/ `/api/download/models/` 下载链接
- ✅ 深色/浅色跟随系统（毛玻璃卡片风格）

### 扩展安装（一次性）
1. Chrome 打开 `chrome://extensions/` → 右上角开「开发者模式」
2. 「加载已解压的扩展程序」→ 选择 `chrome-extension` 文件夹
3. 工具栏固定图标

> 扩展文件夹已随本次更新提供（`chrome-extension/`），内附详细说明。

## 🔧 内置本地桥服务

- 软件启动时自动监听 `127.0.0.1:47531`（仅本机），接收扩展的下载请求并直接入队
- **安全**：只响应 Chrome 扩展与本机页面来源（CORS 白名单），外部网站无法向你的软件提交下载；端口被占用时自动静默跳过，不影响软件本身

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v2.1.1）
