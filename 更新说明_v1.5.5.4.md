# CivitaiFreeTool v1.5.5.4 更新说明（2026-08-11）

> 免费 · 全功能 · 无付费墙

---

## 🔧 本次修复（针对反馈：右键第一张打开"我的文档"、后面的没反应）

### 1️⃣ 修复右键「打开图片所在文件夹」打开"我的文档"
**根因**：调用 `explorer /select,<路径>` 时**路径没有加引号**。当模型路径含空格时（比如你的模型放在带空格的文件夹/文件名里），explorer 解析 `/select,` 参数失败，就会打开默认位置——也就是你看到的**「我的文档」**。

**修复**：改为微软标准写法 `explorer /select,"完整路径"`（路径用引号包裹），**带空格路径也能正确打开并选中目标文件**。

> 顺带说明：你之前的模型路径 `D:\sd-webui-forge-neo\webui\models` 目录本身没空格，但**模型文件名/子文件夹**一旦含空格就会触发；另外之前右键"第一张本地图"打开的是**图片文件所在目录**，现在行为一致且更稳定。

### 2️⃣ 右键操作反馈升级为醒目 toast 提示
之前提示只显示在**底部状态栏**（角落小字），没注意就等于"没反应"。

**现在**：所有右键操作都有**屏幕中间偏下的悬浮提示**（自动消失）：
- ✅ 打开文件夹成功 → 「已打开文件夹」
- ❌ 路径失效 → 显示具体原因
- 🖼️ 右键 C 站远程图点"打开文件夹" → 「该图没有本地文件（可点 🖼️ 下载C站封面 或详情页下载全部图片）」

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v1.5.5.4）
