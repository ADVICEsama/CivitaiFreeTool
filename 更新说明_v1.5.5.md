# CivitaiFreeTool v1.5.5 更新说明（2026-08-09）

> 免费 · 全功能 · 无付费墙

---

## 🆕 新功能

### 📋 粘贴剪贴板链接（批量下载页）
- 新增「📋 粘贴剪贴板链接」按钮：一键读取剪贴板，所有 `http` 开头的链接自动拆行填入输入框（一行一条），复制一批链接 → 粘贴 → 解析，两步搞定

### 📂 右键打开所在文件夹（模型管理）
- 列表 / 瀑布流任意模型右键 → 「📂 打开所在文件夹」→ 资源管理器直接定位并选中该文件

### 🖼️ 模型默认视图（设置）
- 设置 → 界面新增「模型默认视图」（🖼️ 瀑布流 / 📋 列表），**默认瀑布流**
- 启动软件即自动进入你指定的视图，不用每次手动切换

### 🎯 触发词多套独立展示（详情面板）
- 同一个模型有多套触发词（如不同服装）时，**每套独立一行展示**（带序号 ①②③…），和 C 站完全一致
- **点击任意一套 = 单独复制那一个**（不再是一键复制全部）
- 编辑信息弹窗触发词改为**多行文本框**：每行一套，保存后按套存储

## 🐛 修复

- **多套触发词被拆碎**：原来按逗号切分触发词，多套时（如 12 套服装触发词）会被拆成上百个 tag；现在数组整体保存，与 C 站结构一致
- **复制到剪贴板静默失败**：WebView2 在本地文件（file://）环境下 `navigator.clipboard` 不可用，导致详情面板/右键复制一直失败；全部复制改走 Win32 后端，**真正生效**
- **C 站 API Key 申请链接 404**：C 站已迁移账户设置页，链接从 `/settings/api` 更新为 `/user/account`（登录后 Account Settings → API Keys）

## 🧪 验证

- 前端 DOM 测试：默认瀑布流 / 右键菜单含打开所在文件夹 / 触发词 3 套渲染+序号+单套复制 / 粘贴 2 行链接 —— 全过
- 后端单测：剪贴板读写 / 触发词数组不被拆碎 —— 全过
- 回归：WEB INTEGRATION / SCAN FIX —— 通过

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）
