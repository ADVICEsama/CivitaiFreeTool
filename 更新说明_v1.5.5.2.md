# CivitaiFreeTool v1.5.5.2 更新说明（2026-08-11）

> 免费 · 全功能 · 无付费墙

---

## 🆕 本次更新内容

### 1️⃣ 模型管理支持多目录（WebUI / ComfyUI 分开也能用）
之前「模型管理目录」只能填一个路径，很多人 WebUI 和 ComfyUI 的模型放在不同的地方（如 `D:\sd-webui-forge-neo\webui\models` 和 `D:\ComfyUI\models`），只能二选一。

**现在**：设置 → 🔑 基本 → 「模型管理目录（每行一个）」改为**多行输入**，每行填一个文件夹，扫描时自动合并显示；「整理模型」「恢复误整理」也会按文件实际归属的目录处理，不会串目录。

```
D:\sd-webui-forge-neo\webui\models
D:\ComfyUI\models
```

### 2️⃣ 修复：详情图片右键「打开图片所在文件夹」打开的是 C 站网站
之前点这个菜单项会错误地当成网址打开（跳到浏览器），而且导致右键图片的其它操作也受影响。

**现在**：正确调用资源管理器打开图片所在文件夹并选中文件；同时**列表视图**右键菜单也补上了「📂 打开所在文件夹」项。

### 3️⃣ 新增：详情图片右键「复制触发词（tags）」
右键模型详情图片 → 「🏷️ 复制触发词（tags）」，一键复制该模型全部触发词（多套触发词用逗号分隔），直接粘贴进提示词即可用。没有触发词时会提示先去反向解析。

### 4️⃣ 新手引导升级为 5 步（手把手）
之前只有 3 步（主题 / 下载目录 / API Key），很多人看不懂。现在扩展为：

| 步骤 | 内容 |
|---|---|
| 🌗 主题 | 选择界面主题 |
| 📂 下载目录 | 模型下载存放位置（可浏览选择） |
| 🔑 API Key | 申请与粘贴（带图文教程） |
| 📂 **模型目录**（新） | **手把手选择你的本地模型文件夹**（WebUI/ComfyUI 可各填一个，一键浏览选择） |
| 🔄 **反向解析**（新） | 说明用途：识别已下载模型的名字/触发词/封面；「🚀 立即体验（推荐）」一键跳转反向解析页，或「⏭️ 跳过」 |

首次使用会自动弹出；以后想再看：设置页右上角「🎓 新手引导」按钮随时可重开。

### 5️⃣ 悬停显示功能说明（鼠标移上去的小窗）
设置项标签和主要按钮，鼠标悬停 0.5 秒会弹出**功能作用说明小窗**——不知道某个开关/按钮是干嘛的，移上去就看懂了。覆盖 26 个设置项 + 14 个主要按钮（扫描、整理、恢复、下载等）。

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v1.5.5.2）
