# CivitaiFreeTool v1.5.4 更新说明（2026-08-08）

> 免费 · 全功能 · 无付费墙

---

## ⏰ 待办下载清单（新功能）

- 批量下载页「⏰ 待办下载」：记录 Early Access / 付费模型，**到期后打开软件自动弹窗提醒**，一键跳转批量下载页
- **时间自动选择**：C 站返回的 `earlyAccessDeadline` 优先（精确到期），无则默认 7 天
- **解析时自动检测付费模型**：解析到 Early Access / 需付费 → 弹窗选择「⏰ 加入待办」或「💳 仍要下载」

## 💳 下载受限处理（C 站 Early Access / 付费）

- 下载被 C 站拦截（重定向到网页）时**立即识别**，不再白下 0.2MB 再报哈希错误
- 自动弹窗选择：
  - **花费积分 / 重试下载**（可在「🌐 浏览器打开」里购买解锁后重试）
  - **加入待办并移除**（自动进待办、从下载管理移除）
  - **浏览器打开** C 站模型页
- SHA256 校验失败时**保留文件**（应对 C 站 CDN 哈希不一致）

## 🖼️ 自定义封面

- 模型详情页「📤 自定义封面」：选择本地图片 → 自动压缩覆盖为 `<模型名>.preview.png`，列表/瀑布流即时刷新

## ⚙️ 设置同步修复（Issue #2）

- 首次引导修改的主题 / 下载目录 / API Key **立即同步到设置页**（不再显示默认旧值）
- `save_config` 改为真 merge：任何界面保存只更新对应字段，**不再回退其他已设置项**

## 📄 其他

- 批量下载解析完成 → **自动跳转下载管理页**查看进度
- 下载任务列表增加模型链接字段（供受限弹窗/待办使用）

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）
