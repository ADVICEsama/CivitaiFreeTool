# CivitaiFreeTool v2.1.0 更新说明（2026-08-14）

> 免费 · 全功能 · 无付费墙

---

## 🆕 1. Chrome 扩展「一键添加」——浏览器直达工具

在 Chrome 里浏览 Civitai 模型页时，**点击扩展图标，当前页面直接进入 CivitaiFreeTool 批量下载列表**，回到软件点「解析」即可下载。不再需要复制粘贴链接。

- ✅ **点击图标一键添加**：自动校验当前页是 Civitai 模型页（`civitai.red` / `civitai.com`）
- ✅ **右键菜单**：页面或链接上右键 → 「添加到 CivitaiFreeTool」
- ✅ **角标反馈**：✓ 绿色 = 已添加；= 蓝色 = 已在队列（自动去重）；! 红色 = 失败（如软件未打开）
- ✅ **软件实时接收**：打开「批量下载」页，浏览器新添加的链接 3 秒内自动出现在 URL 行，并提示「🌐 已从浏览器添加 N 条链接」
- ✅ **离线不丢**：软件未打开时添加的链接保存在 `browser_queue.json`，下次打开软件进入批量下载页自动合并
- ✅ 支持模型页 / 版本链接（`?modelVersionId=`）/ `/api/download/models/` 下载链接

### 扩展安装（一次性）
1. 打开 Chrome → 地址栏输入 `chrome://extensions/`
2. 右上角开启「开发者模式」
3. 点「加载已解压的扩展程序」→ 选择 `chrome-extension` 文件夹
4. 工具栏固定图标，浏览 C 站模型页点击即可

> 扩展文件夹已随本次更新提供（`chrome-extension/`），内附详细说明。

## 🔧 2. 内置本地桥服务

- 软件启动时自动监听 `127.0.0.1:47531`（仅本机），供扩展投递链接
- **安全**：只响应 Chrome 扩展与本机页面来源（CORS 白名单），外部网站无法写入你的队列；端口被占用时自动静默跳过，不影响软件本身

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v2.1.0）
