# CivitaiFreeTool v2.1.3 更新说明（2026-08-14 · 修复版）

> 免费 · 全功能 · 无付费墙

---

## 🔧 修复：插件下载的模型没有缩略图 / json / 封面

### 问题
用 Chrome 扩展一键下载的模型，下载完成后**没有缩略图**（下载管理列表、移动分类弹窗左侧缩略图位都空着），也没有自动生成 json —— 而手动在软件里批量下载的模型一切正常。

### 原因
插件下载链路（`dl_enqueue_url` 及其变体）入队时**缺少元数据（meta）字段**，导致下载完成后的「自动生成 json + 下载封面」环节拿不到数据而跳过。手动批量下载路径则带上了 meta，所以正常。

### 修复
- 插件下载入队时补全 `meta`（`build_info` + `build_sd_metadata`），与批量下载完全一致
- 任务信息补全 `modelName / versionName` 字段（下载管理列表显示用）
- 下载链接改用与批量下载一致的 `build_download_url`（C 站 `downloadUrl` 字段不可靠）

### 效果（与手动下载完全一致）
- ✅ 下载完成自动生成 SD json（`<名>.json` / `.civitai.info`）
- ✅ 自动下载 C 站封面（`<名>.preview.png`）
- ✅ 下载管理列表缩略图、移动分类弹窗左侧缩略图、模型管理页封面全部正常显示

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v2.1.3）
