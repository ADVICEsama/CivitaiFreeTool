# CivitaiFreeTool v1.5.5.6 更新说明（2026-08-11）

> 免费 · 全功能 · 无付费墙

---

## 🔧 「打开所在文件夹」终极修复（不再乱开桌面/我的文档）

### 为什么之前一直修不好？
Windows 的 explorer 是**单实例**程序：你调用 `explorer /select,路径` 时，参数会被**转交给桌面已经运行的 explorer 进程**处理。这个转交过程会丢失路径引号，路径只要含空格就解析失败 → **回退打开桌面/我的文档**（换 cmd 方式还会闪黑窗）。命令行怎么调都不稳。

### 本次方案：直接调用 Windows Shell API
不再通过命令行，而是直接调用资源管理器「在文件夹中显示」功能的**底层 API**（`SHOpenFolderAndSelectItems`）：

- ✅ **无命令行解析**——路径原样传给 Shell，带空格也 100% 正确
- ✅ **无黑窗**（不再闪 cmd 窗口）
- ✅ 打开并**选中**目标图片文件
- ✅ 已在本机真实测试：带空格路径弹出资源管理器并正确选中

### 附：toast 显示实际路径
右键「📂 打开图片所在文件夹」后，屏幕下方 toast 会显示实际打开的路径（如 `已打开文件夹：D:\sd-webui-forge-neo\webui\models\Lora\xxx.preview.png`）——一眼确认打开的是不是正确位置。

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）

## 🧩 源码

- 仓库：https://github.com/ADVICEsama/CivitaiFreeTool （main 分支，最新 v1.5.5.6）
