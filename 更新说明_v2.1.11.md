# CivitaiFreeTool v2.1.11 更新说明（2026-09-07）

> 免费 · 全功能 · 无付费墙

---

## 🧱 启动健壮性加固：杜绝「打开很多后台进程却没有前端窗口」

### 问题现象
双击 exe 后，任务管理器里出现**好几个** `CivitaiFreeToolWeb.exe` 进程，但一直看不到窗口，插件也连不上（本地桥 47531 无响应）。

### 根因
v2.1.10 的单实例检查放在 `import pywebview / webui` **之后**才执行。当启动过程被卡住时——最常见是：
- `%TEMP%` 被大量旧版打包残留的 `_MEI*` 解压目录塞满（本次清理发现 170 个）
- 杀毒软件对刚解压的大量 DLL 做实时扫描，拖慢 WebView2 初始化

……后续每次双击都**不会走到单实例检查**，而是各自卡在启动途中，不断叠加新的后台进程，窗口始终出不来。

### 修复内容
1. **单实例互斥体检查提前到任何重量级导入之前**（最优先执行）
   - 无论启动多慢、是否卡住，第二次双击都会**立即退出**（或激活已有窗口）
   - 从根本上杜绝「后台进程叠加」
2. **互斥体命名空间增加 `Local\` 回退**：`Global\` 创建失败时仍能去重
3. **WebView2 数据目录固定**到 `%LOCALAPPDATA%\CivitaiFreeToolWeb\webview`
   - 不再每次启动用临时目录，规避临时目录堆积 / 杀软扫描的影响
   - 与其他 pywebview 应用隔离，更稳定

### 如果已经遇到「无窗口 + 很多后台进程」
1. 任务管理器结束所有 `CivitaiFreeToolWeb.exe`（残留的 msedgewebview2 也会随之退出）
2. 清理 `%TEMP%` 下残留的 `_MEI*` 目录（可全选删除，仅临时解压文件）
3. 重新打开软件——本版本起多开会立即去重，不会再叠加

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）
