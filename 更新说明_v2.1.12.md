# CivitaiFreeTool v2.1.12 更新说明（2026-09-08）

> 免费 · 全功能 · 无付费墙

---

## 🛡️ 启动自愈：彻底解决「有后台进程、没有窗口」

### 问题现象
v2.1.11 之后不再叠加多个后台进程了，但**偶发**仍然会出现：双击后任务管理器有一个 `CivitaiFreeToolWeb.exe` 在跑，却始终没有窗口。

### 根因
WebView2 (Chromium) 环境初始化在本机**偶发挂起**——卡在窗口出现之前（多见于显卡驱动异常、虚拟显示适配器、或残留浏览器进程锁住数据目录）。这是环境问题，非软件逻辑问题，但软件应该有能力自愈。

### 修复内容（三重保险）
1. **启动看门狗**
   - 启动后 30 秒内主窗口没出现 → **自动重启一次**（环境变量防无限循环）
   - 重启后仍失败 → **弹窗**列出处理步骤（结束残留进程、关闭占用 WebView2 的程序、重启电脑）
   - 从此不会再有「无声无息卡死」——窗口要么出现，要么给你明确提示
2. **WebView2 数据目录按 PID 独立**
   - 每次启动使用独立目录 `%LOCALAPPDATA%\CivitaiFreeToolWeb\wv2\<pid>`
   - 彻底消除残留/并发浏览器进程锁目录导致的初始化卡死；退出自动清理，7 天前的异常残留自动删除
3. **禁用 GPU 渲染**（`--disable-gpu`）
   - 规避显卡/虚拟显示驱动问题导致的 Chromium 挂死；本地页面软件渲染完全无感

### 📋 启动日志
- 每次启动自动写入 `%LOCALAPPDATA%\CivitaiFreeToolWeb\startup.log`
- 记录每个启动里程碑（单实例检查→模块导入→后端初始化→建窗→看门狗判定），下次再遇到问题可直接把这个文件发给我

---

## 📦 下载

- **GitHub Releases**：https://github.com/ADVICEsama/CivitaiFreeTool/releases
- 配置保存在程序同目录 `user_config.json`（含 API Key，**请勿分享该文件**）