# CivitaiFreeTool 软件介绍 · 口播稿（v2.0 版）

> 口播风格：轻松、有节奏，从"主题与众不同"切入，再逐步展开六大页面。
> 【】内为画面/字幕提示，可根据实际视频画面删改。

---

## 开场 · 从主题切入（约 25 秒）

【画面：软件启动，10 套主题快速轮播切换，最后定格在暮紫主题】

各位好，今天给大家分享一个**光看主题就赢了**的 AI 模型管理工具——CivitaiFreeTool。

你们见过哪个软件，打开设置一数，主题有**十套**？暗色系：深色、**暮紫**、**深海**、**森林**、**熔岩**——五个暗色；亮色系：浅色、**晴空**、**樱粉**、**薄荷**、现代浅色——五个亮色。而且它不是换个颜色敷衍你——**整个氛围背景、按钮、卡片、配色，全部跟着主题联动**。暮紫配紫罗兰流光，深海配天蓝流光，连背景那条流动的色带都跟着变。

更重要的是——**它是免费的，全功能，无付费墙**。

【画面：主页六大导航按钮依次高亮】

那它能干什么？一句话：**下载模型、管理模型、反向解析模型**。我们一个一个页面说。

---

## 一、批量下载（约 30 秒）

【画面：切换到批量下载页，粘贴多个链接，点解析】

玩 SD、玩 ComfyUI 的朋友都懂，模型散落在 C 站和 HuggingFace 上，一个一个下，手都点麻了。

这个页面，**链接往这一贴**，回车自动换行，一次能贴几十条。C 站模型、HuggingFace 仓库、甚至 C 站图片页——**一条图片链接，它能把这张图用到的所有模型全给你扒下来**。

点一下「解析并加入下载队列」——注意，它是**马上跳转到下载管理页**的，不用干等。解析期间按钮锁住，你想重复点都点不了，**再也不会出现同一个模型下三份的惨案**。

碰到 Early Access 付费期的模型，它会自动识别，弹窗问你：加入**到期提醒**，等它免费了软件自动提醒你；还是强行下载。

---

## 二、下载管理（约 20 秒）

【画面：下载管理页，任务进度条在跑】

下载管理这页就一个字：稳。

断点续传、并发下载、失败重试、SHA256 完整性校验——下完的文件它**帮你验证是不是完整的**，下到一半断了？接着下，不用重来。

更贴心的是，下载完成之后，它会自动帮你生成 WebUI 能读的元数据、自动下载封面图，还会问你：**要不要把文件移动到分类文件夹**？你那一堆 Lora、Checkpoint，从此有人帮你归档。

---

## 三、模型管理（核心 · 约 50 秒）

【画面：模型管理页，扫描模型，瀑布流出现】

重头戏来了——**模型管理**。如果你跟我一样，D 盘塞了几百个模型文件，文件名还是什么 "4pricotA" 这种鬼名字，这个页面就是来救你的。

【画面：扫描后，缩略图瀑布流 + C 站模型名】

点一下「扫描模型」，你的模型**全部带封面、带 C 站原名**出现在瀑布流里。看到封面才想起来："哦——原来是这个！"

然后看这排按钮：

【画面：改名按钮的二级菜单展开】

**改名**——自定义改名、**一键改成 C 站模型名**、或者**翻译成中文文件名**，支持批量。几百个模型，一次选中，全改完。

【画面：整理模型 + 恢复误整理】

**整理模型**——先选好你的目标环境是 WebUI 还是 ComfyUI，然后三种模式任选：手动一个个选文件夹、按 C 站分类自动归档、或者按你自己的关键词规则。它会按 `Lora / 基础模型 / 分类` 两级目录给你排得整整齐齐。排错也不怕，有**「恢复误整理」**，按移动日志一键回退——只移动，不删文件，安全感拉满。

【画面：右键菜单】

每个模型右键，菜单从复制 C 站名、打开所在文件夹，到识别模型信息、移入回收站，全是大白话，**鼠标一悬停还有小字解释**，新手也不用猜。

---

## 四、反向解析（约 30 秒）

【画面：反向解析页，添加文件夹，开始反查】

最绝的功能来了——**反向解析**。

你硬盘里那些**来路不明、名字乱码**的模型文件，拖进来，点一下「开始反查」，它按 SHA256 去 C 站匹配——**名字、触发词、类型、封面，全给你找回来**。反查完，模型管理里每个模型都有名有姓有触发词，复制就能用。想批量整理的，先反查一遍，再整理，事半功倍。

---

## 五、工作流分析（约 20 秒）

【画面：工作流分析页，拖入 json，三栏结果】

再看这个——**工作流分析**。把 ComfyUI 的工作流 json，或者带内嵌工作流的 png，直接拖进去。

左边是节点列表，右边是**它引用的所有模型**，而且自动跟你的本地文件比对——哪个模型你本地有、哪个没有、SHA256 对不对，一目了然。别人分享的工作流，拿到手先拖进去看看，缺什么模型马上知道。

---

## 六、细节彩蛋（约 25 秒）

【画面：右键复制图片、到期提醒、关于弹窗】

最后说几个用了就回不去的细节：

**详情页的图片**——右键直接**复制图片本身**、复制**正面提示词**、复制**负面提示词**，本地 PNG 元数据都能识别，做笔记、发图、抄参数，效率翻倍。

**到期提醒**——Early Access 的模型加入待办，免费时间一到，打开软件自动提醒。

**新手引导**——第一次打开有六步引导，从选主题到填目录手把手教你；点功能介绍卡片还能直接跳进对应页面，引导缩小成小窗挂屏幕边上，不打断你。

**关于页**里还有 GitHub 直达——源码开源，更新迭代是真的快。

---

## 结尾 · 号召（约 20 秒）

【画面：软件主界面 + 下载方式】

所以，如果你也在为几百个模型的管理头疼，或者想找一个**免费、好看、全功能**的 C 站下载工具——

CivitaiFreeTool，值得你试一下。

下载方式我放在简介和评论区置顶了，GitHub 上也有完整源码和文档。觉得有用的话，**一键三连**，让更多玩模型的兄弟看到它。我们下期再见！

---

## 附：口播时长参考

| 段落 | 时长 |
|---|---|
| 开场（主题切入） | ~25s |
| 批量下载 | ~30s |
| 下载管理 | ~20s |
| 模型管理（核心） | ~50s |
| 反向解析 | ~30s |
| 工作流分析 | ~20s |
| 细节彩蛋 | ~25s |
| 结尾号召 | ~20s |
| **合计** | **约 3 分 40 秒** |
